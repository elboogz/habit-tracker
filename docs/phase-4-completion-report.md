# Phase 4 Completion Report — The Recovery Flow

Implements `docs/phase-4-plan.md` (the approved plan, including the suppression revision) in full. All ten commits in the approved sequence (§9) are complete. Phase 5 has not begun — no AI coaching changes, no notification copy, no reflection-prompt rewrites, no challenge-tolerance changes.

## Commits completed

1. **`1dd192b`** — data model + migrations, no behaviour change. `LapseReasonKey`/`LapseReasonEntry`, `HabitState.lapseReasons`, `Habit.reducedTarget`, `HabitLog.reduced`, `migrateLapseReasons`, and `supabase/phase-4-recovery-flow-schema.sql`.
2. **`100e518`** — domain logic. `openLapse`, `lapseReasonSuppressionUntil` (`lib/domain/recovery.ts`), `nextScheduledOpportunityAfter` (`lib/domain/schedule.ts`), `reducedTargetFor` and reduced-aware `isDoneOnDay` (`lib/domain/habit-stats.ts`), new test fixtures, Edge Function regeneration + hand-fix (see Deviations).
3. **`c9179f2`** — sync plumbing. `lapse_reasons` row mapping, the two new habit/log columns, `mergeRemote` for `lapseReasons`, five new store actions (`addLapseReason`, `addSchedulePeriod`, `pauseHabit`, `logReducedCompletion`, `setLogAmountForDate`).
4. **`f46b3ad`** — recovery card shell. `components/recovery-card.tsx`, `lib/recovery-card-dismissals.ts`, wired into Today with Continue/Skip/Dismiss live, three stub rows.
5. **`b5bcdee`** — Do a smaller version. `habit-form.tsx` stepper, `logReducedCompletion` wiring, reduced-recovery celebration copy.
6. **`8c4b459`** — Pause this habit wiring.
7. **`c426f11`** — schedule editor in `habit-form.tsx`, "Adjust the schedule" wired to navigation.
8. **`4043758`** — Reflect flow (inline five-chip prompt + internal Skip).
9. **`1cd401e`** — retroactive entry (`HabitCalendar.onDayPress`, `DayCorrectionPanel`, `setLogAmountForDate` wiring).
10. **This commit** — documentation (this report + `CLAUDE.md` update).

## Files and schemas changed

**New files:** `components/recovery-card.tsx`, `lib/recovery-card-dismissals.ts`, `supabase/phase-4-recovery-flow-schema.sql`, `docs/phase-4-plan.md`, `docs/phase-4-completion-report.md`.

**Modified:** `lib/habit-types.ts`, `lib/domain/persistence.ts` (+ test), `lib/domain/recovery.ts` (+ test), `lib/domain/schedule.ts` (+ test), `lib/domain/habit-stats.ts` (+ test), `lib/habit-store.tsx`, `lib/supabase-sync.ts`, `lib/sync-queue.ts`, `app/(tabs)/index.tsx`, `app/habit-form.tsx`, `app/habit/[id].tsx`, `components/habit-calendar.tsx`, `supabase/functions/ai-insights/index.ts`, `supabase/functions/send-coaching-push/index.ts`, `CLAUDE.md`.

**Schema:** `lapse_reasons` table (RLS scoped to `auth.uid() = user_id`, append-only from the client) plus `habits.reduced_target` and `habit_logs.reduced` columns — all in `supabase/phase-4-recovery-flow-schema.sql`. **Confirmed applied** to the live database by the user during commit 1.

## Tests added or updated, and results

Extended `lib/domain/recovery.test.ts`, `lib/domain/schedule.test.ts`, `lib/domain/habit-stats.test.ts`, `lib/domain/persistence.test.ts` with fixtures for: `openLapse` mid-run and edge cases; a reduced completion closing a lapse identically to a full one; the §7.4 retroactive-removal worked example (a completion that had closed a lapse and fired a Recovery Event is removed, reopening the lapse and un-firing the event — proven directly, not just asserted); `nextScheduledOpportunityAfter` for daily, weekday-limited, and indefinitely-paused habits; `lapseReasonSuppressionUntil` across several `LapseReasonEntry` timings within an open lapse; `reducedTargetFor`'s derivation and clamping; `isDoneOnDay`'s reduced-awareness and backward compatibility with pre-Phase-4 logs.

**Final verification run** (after commit 9, repeated after the CLAUDE.md update in commit 10):
- `npx tsc --noEmit` — clean, no errors.
- `npx jest` — **124/124 tests passed**, 8 suites, including the pre-existing edge-function freshness check (`scripts/build-edge-functions.test.ts`), which passed because the regenerated Edge Functions were committed rather than left stale.
- `npm run lint` — clean, exit 0.

All three commands were actually run at each commit boundary, not assumed.

## Migration instructions and rollback

Already applied — included here for the record. Run in the Supabase SQL Editor:

```sql
create table if not exists public.lapse_reasons (
  id text primary key,
  user_id uuid not null references auth.users(id) on delete cascade,
  habit_id text not null,
  missed_opportunity_date date not null,
  reason text check (reason in ('too_busy','forgot','low_energy','not_feeling_it','something_else')),
  note text,
  skipped boolean not null default false,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create index if not exists lapse_reasons_user_id_updated_at_idx on public.lapse_reasons (user_id, updated_at);
create index if not exists lapse_reasons_habit_id_idx on public.lapse_reasons (habit_id);

alter table public.lapse_reasons enable row level security;
create policy "Individuals can manage their own lapse reasons" on public.lapse_reasons
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);

alter table public.habits add column if not exists reduced_target int;
alter table public.habit_logs add column if not exists reduced boolean not null default false;
```

**Rollback** (safe at any time — no other table depends on `lapse_reasons`, and the two columns fall back to the pre-Phase-4 default every consumer already treats as baseline):

```sql
drop table if exists public.lapse_reasons;
alter table public.habits drop column if exists reduced_target;
alter table public.habit_logs drop column if exists reduced;
```

## Edge Functions

`supabase/functions/ai-insights/index.ts` and `supabase/functions/send-coaching-push/index.ts` were regenerated (`npm run build:edge-functions`) at commit 2, because the reduced-aware `isDoneOnDay` is in the generated whitelist. **Already re-pasted and deployed** to the Supabase Dashboard by the user, confirmed during commit 2 — not a deferred action.

## Deviations from the approved plan

1. **Edge Function hand-fix beyond the generated block (commit 2).** The plan's edge-function regen step anticipated only the generated block changing. While verifying the regen, I found both Edge Functions also hand-maintain their own `LogRow`/`HabitLog` row-mapping types and explicit `.select()` column lists *outside* the generated markers. Without updating those, the new `reduced` field would never reach the regenerated `isDoneOnDay` server-side, silently defeating the point of the commit. I hand-fixed both (`LogRow.reduced`, `HabitLog.reduced`, and added `reduced` to both `.select()` calls). This is a correctness necessity for what commit 2 already required, not new scope, but it's a real deviation from "only the generated block changes" and is reported here per instruction.
2. **§7.1/§7.2 resolved textual ambiguity (commit 9).** §7.1's pressable-range formula (`date >= addDays(dayKey(), -6)`) literally includes today, but §7.2 explicitly reasons that today should be excluded from the retroactive panel ("today already has a first-class, always-visible editor and duplicating it here would be redundant"). I implemented the more explicitly-reasoned interpretation: the 6 days *before* today are pressable, today is not. This is a resolution of an internal inconsistency in the approved text, not a scope change — flagged here rather than silently picked.
3. **On-device/browser interaction verification.** Per an explicit question to the user after commit 4 (the app requires signing in against the real Supabase project, and I had no test credentials), the user chose "code + automated checks only" for commits 4–9. Every UI-touching commit's completion is therefore backed by TypeScript, automated tests, and code-path inspection, **not** a manual on-device pass — see "Verification methods" below for the one partial exception. A full on-device acceptance pass is still recommended before considering Phase 4 user-facing-ready; see "Remaining risks."

No other deviations. The commit order, all six approved product decisions (§8.1–§8.6), and the six-option list match the plan as written.

## Verification methods, per completion requirement

- **Recovery card appears only when `openLapse !== null` AND today `isScheduledOpportunity` AND not suppressed** — code-path inspection of `eligibleHabits` in `app/(tabs)/index.tsx`, matching §3.1's formula exactly. Not manually verified on-device.
- **All four card actions suppress until the next Scheduled Opportunity** — code-path inspection (`recoveryCardSuppressedUntil`, `handleRecoveryContinue`/`handleRecoverySkip`/`handleReflectChoice`/`handleReflectSkip`, all keyed through `nextScheduledOpportunityAfter` or `lapseReasonSuppressionUntil`). Not manually verified on-device.
- **Skip writes `skipped: true`; Reflect writes the selected reason** — code-path inspection + TypeScript validation of the `addLapseReason` call sites. Not manually verified on-device.
- **Continue and Dismiss record no behavioural fact** — code-path inspection; both call only the local `dismiss()` setter from `useRecoveryCardDismissals`, never `addLapseReason`.
- **Reduced completions available only for habits with measurable targets** — automated test (`reducedTargetFor` suite in `habit-stats.test.ts`) + code-path inspection of the `reducedTargetFor(habit) !== null` render guard in `recovery-card.tsx`.
- **Schedule editor creates new effective-dated periods without editing past periods** — code-path inspection (`addSchedulePeriod` always appends a new period via the reducer, never mutates an existing one) backed by the pre-existing Phase 2 append-only design and its test coverage in `schedule.test.ts`.
- **Retroactive entries limited to the last 7 days, correct recomputation** — code-path inspection of `editableFrom` in `habit-calendar.tsx` + automated test ("example G" in `recovery.test.ts`, which proves recomputation directly rather than asserting it).
- **Retroactive removal reopens a lapse and reverses a Recovery Event** — automated test, same "example G" fixture, asserting the before/after state of `recoverableLapseInstances`, `closedLapses`, and `recoveryEvents` explicitly.
- **No shame-framed messaging, absence day-counts, or warning-coloured elements** — manual copy review of every new user-facing string across `recovery-card.tsx`, `habit-form.tsx`, `habit/[id].tsx`, `index.tsx` (grepped for miss/fail/broke/streak/shame/behind/late/overdue framing — none found beyond the pre-existing, unmodified "streak" label); manual review of styling confirms no red/orange/warning colors were introduced, only the app's existing `tint`/`icon`/`background`/`text` theme colors.
- **Full test suite / TypeScript checks** — actually run at every commit boundary; see "Tests" above for the final results.
- **Web build sanity check (commit 4 only)** — after wiring `RecoveryCard` into Today, started `npx expo start --web` and drove it with headless Playwright: the app bundles and reaches the sign-in screen with zero console errors and zero page errors. This proves the new component and its imports compile and don't crash the bundle; it does **not** exercise the card's actual interactions (that requires a signed-in session against the real Supabase project, which I don't have credentials for).

## Remaining risks / deferred decisions

- **On-device acceptance pass.** Per the user's explicit choice, no interactive on-device/browser verification was performed for the recovery card, schedule editor, Reflect prompt, or retroactive-entry panel. All are backed by TypeScript + automated domain tests + careful code-path inspection, but a real device or authenticated-browser pass is the recommended next step before treating Phase 4 as user-facing-complete.
- **Cross-device / reinstall gap for Continue/Dismiss** — explicitly accepted in §8.6; bounded to at most one extra card appearance, one Scheduled Opportunity's worth of exposure.
- **`lib/recovery-card-dismissals.ts` storage is not scoped per-user**, unlike the main `habit-tracker/state-v1:<userId>` key. On a device shared across multiple signed-in accounts, a stale dismissal entry from one account is technically readable (though not writable in a way that matters) by another. Practically harmless — habit IDs are globally-unique UUIDs, so cross-account key collision is not a realistic concern — but it's an inconsistency with the rest of the app's per-user storage convention worth knowing about. Not fixed: the plan doesn't call for per-user scoping here, and §8.6 already frames this store's entries as harmless and orphan-tolerant by design.
- **Final copy** for the recovery card, reduced-completion celebration, and lapse-reason prompt is illustrative, per §12 — written on-brand with the existing app's tone (the codebase's own em-dash-forward style, no shame framing), not final-polished copy.
- **Phase 5 has not begun.** `lapse_reasons` is written but read by nothing yet, exactly as designed — it exists to feed Phase 5's coach.

## Confirmation

Working tree is clean as of this commit (verified before pushing). All Phase 4 commits are pushed to `origin/main`. An annotated tag `phase-4-complete` is created at this documentation commit and pushed to the remote.

## Post-completion manual testing fix

**Observed inconsistency.** During manual on-device testing after `phase-4-complete`, the Progress screen for a habit backfilled via the Settings developer tools (e.g. "Fill all") simultaneously showed substantial history (31 Total Completions, 12 of the last 14 days completed) and Momentum State "Getting started" / `insufficient_data` copy ("Still gathering enough days to show a pattern.") — an internally contradictory result for the same habit at the same moment.

**Root cause.** `lib/domain/schedule.ts`'s `scheduledOpportunitiesUpTo` (the foundation every Momentum and Recovery calculation is built on) never generates a Scheduled Opportunity before `localDayKeyOf(habit.createdAt)` — this floor is the correct, locked Phase 2 rule and was not changed. The developer simulation reducer cases in `lib/habit-store.tsx` (`debugBackfillLogs`, `debugAdvanceChallenge`, `debugCompleteChallenge`, `debugFillHistory`) backdated `HabitLog.date` into the past to simulate history, but never touched the habit's own `createdAt`, which stayed at its real value (the moment the habit was actually created via the app, typically "today" for a habit created solely to test these tools). As a result:
- `totalCompletions` and `consistency` (`lib/domain/habit-stats.ts`) read raw logs by calendar date and are unaffected by `createdAt` — they correctly reflected the full backfilled window.
- `candidateStateAt`/`confirmedStateAt` (`lib/domain/momentum.ts`) and the recovery functions in `lib/domain/recovery.ts` all derive their opportunity list from `scheduledOpportunitiesUpTo`, which only walked forward from "today" (the real `createdAt`) — so they saw exactly one Scheduled Opportunity and correctly, per the locked rules, reported `insufficient_data`.

Both readings were individually correct given their inputs; the inputs themselves (a backfilled log history paired with an unbackdated creation date) did not represent a coherent simulated habit. Progress was not caching or reading stale state, and no domain rule (Momentum thresholds, hysteresis, `insufficient_data`'s definition, Recovery Rate/Event semantics) was defective.

**Fix.** Added `lib/domain/dev-simulation.ts`'s `backdatedCreatedAt(createdAt, simulatedDates)` — a pure, dev-tool-only helper that computes the `createdAt` needed so every simulated date is a genuine Scheduled Opportunity (backdating to local midnight of the earliest simulated date, preserving the original time-of-day; a no-op if `createdAt` already covers the window; never moves `createdAt` *later*, so a habit whose real creation date already predates the simulated dates — e.g. "Simulate streak" on a month-old habit — is left untouched). Wired it into all four `lib/habit-store.tsx` debug reducer cases via a small `withSimulatedHistory` wrapper that also bumps `updatedAt` to `now` when `createdAt` actually changes, applied only to the specific habit(s) each action is already backfilling — no other habit's data is touched.

**Tests added** (`lib/domain/dev-simulation.test.ts`, 13 cases):
- `backdatedCreatedAt` unit behavior: no-op when already covered, backdates to the earliest date while preserving time-of-day, never moves `createdAt` later.
- A simulated 30-day habit history with 12-of-14 recent completions no longer reads `insufficient_data`, and matches a manually constructed genuine-history fixture's `candidateStateAt`/`confirmedStateAt`/`consistency`/`totalCompletions` output exactly (also reproduces the original bug against the un-fixed `createdAt` first, to prove the test actually exercises the failure).
- A sparse/new simulated habit (1 backfilled day) correctly remains `insufficient_data` — the fix doesn't weaken the threshold.
- A simulated recovery/lapse history produces identical `recoveryEvents`, `closedLapses`, and `recoveryRate` to an equivalent manually constructed genuine fixture.
- Simulated history still respects the habit creation-date floor (no opportunity generated before the backdated `createdAt`).
- A pause period inside the simulated window is still excluded correctly after backdating.
- Backdating never produces a Scheduled Opportunity after the real "today", and a simulated date later than `createdAt` can never push it forward.

Full suite: `npm test` — 9 suites, 137 tests, all passing (13 new). `npx tsc --noEmit` — clean. `npm run lint` — clean.

**Production impact: none.** The fix is confined to `lib/domain/dev-simulation.ts` (new file, not part of the generated Edge Function whitelist in `scripts/build-edge-functions.js`) and the four `debug*` reducer cases in `lib/habit-store.tsx`, which are reachable only from Settings' `__DEV__`-only developer tools section. No Momentum, Recovery, schedule, or consistency rule changed; no schema change; no Edge Function regeneration required (neither `day-key.ts` nor `habit-stats.ts` — the only two files the generator inlines — were touched). A real user's own habit history, created and logged entirely through normal app use, is completely unaffected.

## Post-completion fix 2: Momentum evaluation precedence + developer scenario simulator

**Second manual-testing report.** After confirming the createdAt fix above resolved the original stale-history issue (a "Simulate 30-day history" habit correctly left "Getting started"), a second report came from a fresh habit ("Read") backfilled with the Settings "+6-day streak" tool: Progress showed 6 Total Completions and 6-of-7 recent days completed, yet still displayed Momentum State "Getting started" / `insufficient_data`. The request was to determine whether this was the same class of bug (a debug tool bypassing the shared simulation path) and, if so, unify the tools; separately, to design a developer-only scenario simulator for recovery/momentum testing.

**Investigation: not a simulation-path bug.** `debugBackfillLogs` (the "+6-day streak" reducer case) already called the same `withSimulatedHistory`/`backdatedCreatedAt` helper as every other debug action — all four were fixed together in the previous commit. Reproducing the exact scenario with a **genuinely** 6-day-old habit (real `createdAt`, no dev tool involved at all) produced the identical "Getting started" result, proving the dev tool and real usage are behaviorally identical here. The actual cause: `confirmedStateAt`'s hysteresis requires 3 consecutive identical daily candidate readings before a new state displays. With 6 backfilled days plus an unlogged "today," the candidate sequence is `building, building, steady, steady, building` — the trailing open day reverts the reading before "steady" ever holds 3 in a row. A 7-day streak (one more day of margin) does confirm "Steady" — verified directly. This is a real, reproducible property of the existing (Phase-2-approved) Momentum thresholds, not a divergence between simulated and genuine history, so the "+6-day streak" tool itself was left unchanged.

**A genuine defect found during that investigation: `rebuilding` was unreachable as a *confirmed*, displayed Momentum State.** While constructing test histories to design the new scenario simulator's "Rebuilding" button, no history — of any length or shape — ever produced a confirmed `rebuilding` badge. Verified by exhaustively evaluating `confirmedStateAt` against **every possible daily hit/miss history up to 14 Scheduled Opportunities** (a full binary search, not a sample): zero histories confirmed `rebuilding`, despite `rebuilding` being a real, spec-named state (`docs/phase-2-implementation-plan.md` §3's threshold table, row `rebuilding`; the "long_term_improving_trajectory" fixture in §8 explicitly expects a confirmed sequence passing through `rebuilding`).

- **Root cause.** `candidateStateAt`'s evaluation order (`lib/domain/momentum.ts`) checked `building` before falling back to `rebuilding`. `building`'s bar is low (a 60% completion rate over just a 3-opportunity window, and only required to *improve* on the prior window when one exists) — low enough that it claimed nearly every day immediately following a qualifying (≥3-miss) lapse's recovery, before `rebuilding`'s own 3-opportunity confirmation window could ever complete. `rebuilding` was reachable as a one-day *candidate* (already covered by an existing test) but never as a 3-in-a-row *confirmed* state.
- **Verification against the approved definition, before changing anything.** Per the plan's own worked example (`long_term_improving_trajectory`: "confirmed sequence: `insufficient_data` → `quiet`/`rebuilding` → `building` → `steady` → `thriving`"), `rebuilding` is intended to be reachable as a confirmed state on the way up from a longer lapse. The evaluation-order line elsewhere in the same document (`insufficient_data → recovering → quiet → best of {thriving, steady, building} → rebuilding fallback`) is the textual source of the bug — read literally, `building` was always checked ahead of `rebuilding`, which the exhaustive search shows makes `rebuilding` unreachable in practice, contradicting the plan's own worked example. This is a defect in the implementation's fidelity to the plan's own stated intent, not a threshold value that needed changing.
- **Fix — the smallest change that resolves the contradiction.** In `candidateStateAt`, `isRebuilding` is now checked immediately after `thriving`/`steady` (which legitimately still take precedence — sustained strong evidence should supersede a trailing "still rebuilding" read) and *before* `meetsBuilding`. No threshold, window, confirmation count, or entry condition for `building`, `steady`, `thriving`, `recovering`, `quiet`, or `insufficient_data` was touched — only the position of one `if` relative to another.
- **Tests added** (`lib/domain/momentum.test.ts`, new "rebuilding evaluation precedence" describe block, 6 cases): a ≥3-miss lapse's recovery still reaches candidate `rebuilding` (pre-existing case, re-verified); `rebuilding` becomes confirmed only on the 3rd consecutive agreeing candidate, not before (both the "not yet" and "now confirmed" days asserted); a short (≤2-miss) lapse still resolves to `recovering`, never `rebuilding`, at both candidate and confirmed level; ordinary early progress with no preceding lapse still confirms `building`, unaffected; `steady`/`thriving` retain their existing classification; a currently-open lapse still confirms `quiet`. All pre-existing fixtures (137 tests across the full suite before this change) continued to pass unmodified — none of them had encoded the precedence bug as an expected value.
- **Reachability re-proof.** Re-ran the exhaustive search after the fix: every one of the 7 `MomentumStateKey` values (`insufficient_data`, `building`, `steady`, `recovering`, `rebuilding`, `thriving`, `quiet`) is now reachable as a confirmed state via some valid daily history at length ≤ 10 (witness patterns recorded in the commit; shortest for `rebuilding` is a 7-day history: 4 misses then 3 held completions).
- **Edge Function impact: none.** `momentum.ts` is not part of `scripts/build-edge-functions.js`'s `SOURCES` whitelist (only `day-key.ts` and `habit-stats.ts` are inlined into the Edge Functions) — no regeneration or re-paste needed. Confirmed by inspection, not assumed.
- **Production impact: real, and disclosed.** Unlike the createdAt fix above, this changes actual domain behavior: any real habit whose history matches the previously-misclassified shape (recovering from a lapse of 3+ missed days) will now correctly read "Rebuilding" on Progress instead of whatever it previously settled on (typically "Building" or "Steady," depending on what followed). This is a bug fix bringing behavior in line with the already-approved Phase 2 spec, not a new product decision — but it is a real, user-visible classification change and is called out explicitly here rather than folded silently into the dev-tools work that motivated finding it.

**Separately flagged, not fixed: `insufficient_data` persistence and "today" handling.** `docs/phase-2-implementation-plan.md` §3 defines a miss as "a Scheduled Opportunity for `date < today` with no qualifying Completion — **today is never classified as missed**." `lib/domain/recovery.ts`'s functions satisfy this by construction (today can only ever appear as the *resolving* side of a pairwise check, never the *missed* side, and `openLapse` explicitly evaluates only through yesterday). `lib/domain/momentum.ts`'s `candidateStateAt`, however, builds its rate-window records directly from `scheduledOpportunitiesUpTo` including today, and an unlogged today reads as `completed: false` in that window — actively lowering the day's own completion rate rather than being excluded, which is what produces the six-day-streak-plus-open-today "Getting started" behavior investigated above. This is a real, textual deviation from the spec's own "today is never classified as missed" wording, not merely a surprising-but-intentional threshold effect — but per explicit instruction it was **not** changed here: the "+6-day streak" tool was proven behaviorally faithful to real usage (not a simulation defect), and altering how Momentum treats today is a separate, more invasive change to `lib/domain/momentum.ts`'s core evaluation than the rebuilding-precedence fix, touching every candidate-state check rather than reordering two of them. Flagged for a future, deliberate review rather than bundled into this fix.

**Developer scenario simulator (new feature).** Added `lib/domain/dev-simulation.ts`'s `ScenarioKey`/`scenarioPattern`/`simulatedLogsFor` and a new `debugSimulateScenario(habitId, scenario)` store action, exposed in Settings' developer tools as a "Simulate a scenario" section (one row per habit, 8 scenario buttons each): **Miss yesterday**, **Miss 2 days** (both open a real, still-unresolved lapse and leave today unlogged, so the Recovery Card renders and a live tap on Today exercises the actual celebration), **Recover today**, **Recover after a lapse** (both close a lapse with today's own completion, for an instant Recovery Rate/Recovery Time/Recovery Count preview), and **Quiet stretch**, **Rebuilding**, **Building**, **Thriving** (each an "instant preview" history that confirms the matching Momentum State badge). Every pattern was derived empirically against the real domain functions (`openLapse`, `recoveryEvents`, `closedLapses`, `confirmedStateAt`) — not hand-guessed — and is proven in `lib/domain/dev-simulation.test.ts`'s new "scenario simulator" describe block (8 cases, one per scenario, each asserting the specific domain output the button exists to exercise). No scenario bypasses or special-cases the domain layer: each is a `SimulatedDay[]` pattern (dates + a completed flag) fed through the exact same `backdatedCreatedAt` + log-generation pipeline as the pre-existing debug tools, so it reads through `openLapse`/`momentum.ts`/`recovery.ts` exactly as real usage would.

As a byproduct — and directly addressing the original "unify the debug tools onto a single shared simulation mechanism" request, though the investigation above found no *behavioral* divergence to unify — `simulatedLogsFor` in `lib/domain/dev-simulation.ts` is now the one place that decides what a simulated completion log row looks like. All five debug reducer cases (`debugBackfillLogs`, `debugAdvanceChallenge`, `debugCompleteChallenge`, `debugFillHistory`, `debugSimulateScenario`) call it instead of five near-identical inline `.map(...)` blocks, removing the duplication that made the original "did one tool drift from the others" question hard to answer with confidence in the first place.

**Full verification.** `npm test` — 9 suites, 151 tests, all passing (14 new: 6 in `momentum.test.ts`, 8 in `dev-simulation.test.ts`). `npx tsc --noEmit` — clean. `npm run lint` — clean. `npx expo export --platform web` — bundles cleanly (1288 modules, all 15 static routes including `/settings` present), confirming the new Settings UI compiles into a real bundle, not just passing the type checker.

## Post-completion fix 3: Momentum confirmation mechanism (today-completion monotonicity)

**Request.** Alongside the exhaustive 7-state reachability search from fix 2, add a monotonicity check: for every history in the search space, completing the current day must never move the candidate or confirmed Momentum State to a less favourable trajectory solely because the day changed from open to completed — with the comparison relation defined explicitly and grounded in the approved specification, not assumed to be a total order.

**The comparison relation.** Only four of the seven `MomentumStateKey` values form a defensible chain: `insufficient_data < building < steady < thriving`, because each step requires strictly more/stronger positive evidence than the last (window 0/3/5/8 opportunities, minimum completion rate —/60%/80%/90%, per `MOMENTUM_CONFIG` — an explicit, nested design, not inferred). `recovering`, `rebuilding`, and `quiet` are classified by lapse recency/length, not a rate bar, and the product deliberately avoids a single "goodness" framing across all seven states (`docs/phase-3-experience-plan.md` §7.1's non-shame labeling) — so they were left out of the chain, not force-ranked. A narrower fallback invariant ("never enter `{quiet, recovering, rebuilding}` from outside that set purely by completing today") was tried and rejected: an exhaustive sweep showed it produces ~1000 "violations" that are `building → recovering`/`rebuilding`, which is not a bug — `recovering`/`rebuilding` are *defined* as "you just completed today, closing a recent lapse," so completing today is a structural precondition for earning that label, not a corruption of an otherwise-fine trajectory. Only the chain-comparable-pair check was kept.

**What the exhaustive check found.** Comparing `candidateStateAt`/`confirmedStateAt` with "today" left open vs. completed, over every possible daily hit/miss history up to a 12-day prefix (1027 candidate-level and 2053 confirmed-level comparable pairs): 0 violations at the candidate level, but **1 at the confirmed level** — a habit completed on days 1–4 (candidates `insufficient_data, insufficient_data, building, building`) reads confirmed `building` if day 5 ("today") is left open, but confirmed `insufficient_data` — strictly *lower* — if day 5 is completed instead. Root cause: `computeConfirmedState`'s pending-transition counter resets to 1 on *any* change of candidate value, even when the new candidate (`steady`, day 5's own reading once completed) is chain-comparable and *stronger* than the one it was tracking (`building`, at pending count 2/3) — discarding that progress rather than crediting it.

**A rejected first fix.** A version that let a pending run continue past a stronger candidate by tracking a "floor" (the lowest rung seen) and confirming it once held for 3 opportunities resolved the target case, but broke the **locked** `perfect_completion_history` fixture (`docs/phase-2-implementation-plan.md` §8: "confirmed at opportunity 10" for `thriving`) — traced and confirmed directly: `thriving` confirmed at opportunity 11 instead, and `steady` at opportunity 8 instead of 7. This wasn't an implementation slip; it's inherent to that design — any rung that "owns" and consumes 3 opportunities of confirmation necessarily delays the next, stronger rung's own fresh count, for *any* smoothly improving trajectory, not just the pathological case. Reverted; confirmed the revert restored all 151 tests and reproduced the exact original violation (open → `building`, completed → `insufficient_data`) before proceeding.

**The mechanism implemented instead — `computeConfirmedMomentumState`, replacing `computeConfirmedState` only for `confirmedStateAt`** (the generic, exact-match `computeConfirmedState` is untouched and still exported/tested for arbitrary synthetic sequences): confirmation as a property of the **trailing `transitionConfirmationOpportunities` (3) candidates**, evaluated fresh at every opportunity, not a stateful pending counter one rung consumes:

1. If all 3 trailing candidates are identical, confirm that value — unchanged from before; the *only* rule governing the off-chain states (`quiet`/`recovering`/`rebuilding`), and the *only* rule that can ever lower the confirmed state (a decline within the chain still needs 3 consecutive identical weaker candidates).
2. Otherwise, if all 3 trailing candidates *and* the current confirmed state are chain-comparable, take the window's minimum rank. If strictly higher than the confirmed state's rank, raise to it. Only ever raises; never fires against an off-chain confirmed state.
3. Otherwise, unchanged.

This doesn't weaken the 3-opportunity requirement: it's satisfied as "the trailing 3 opportunities each showed *at least* this much evidence" — Rule 2 takes the window's *minimum*, so it can only confirm a rung at or below what all 3 support, never stronger than any one of them individually earned (unlike the rejected first fix, which let a stronger state confirm off fewer than 3 of its own occurrences).

**Verification, all performed before committing:**
- `perfect_completion_history` traced opportunity-by-opportunity: `steady` confirms at opportunity 7, `thriving` at opportunity 10 — both **exactly the locked values**, unchanged. (`docs/phase-2-implementation-plan.md` §8 pins only these two numbers for this fixture, not the full intermediate sequence — the new mechanism does introduce an early, previously-unpinned `building` reading at opportunities 5–6, filling in territory the fixture never specified, not breaching it.)
- The target case: open and completed both now read confirmed `building` — equal, not worse.
- A genuinely-established `thriving` confirmation (10 perfect days) holds after a single subsequent miss — decline still requires 3 consecutive weaker candidates, proven directly, not just asserted.
- Three required transition sequences, run directly against `computeConfirmedMomentumState`: `insufficient_data, building, thriving` (a non-adjacent upward move) doesn't confirm off only 2 samples; `building, recovering, building` (an off-chain state interrupting a run) doesn't bridge across it; `building, building, insufficient_data, building` (a downward move) restarts the count from zero.
- The exhaustive today-open-vs-completed sweep re-run: **0 violations at both candidate and confirmed level**, over 1027 and 2053 comparable pairs respectively (up to 12-day prefixes).
- The 7-state reachability search re-run: every witness pattern is **identical** to before this fix, including `rebuilding`'s shortest witness (`0000111`, 7 days) — no state's reachability or shortest witness changed.
- All 22 existing `momentum.test.ts` tests pass **unmodified** (zero test files edited to accommodate the change) — the new mechanism reproduces every previously-approved fixture exactly, plus corrects the one found gap.

All of the above (plus the enumerated unranked candidate/confirmed transition pairs, informational, not asserted) are committed as permanent tests in `lib/domain/momentum.exhaustive.test.ts`, kept separate from `momentum.test.ts`'s fast fixtures since the exhaustive sweeps take ~7s.

**Production impact: real, and disclosed precisely.** Any real habit history where a chain-comparable, stronger candidate (e.g. `steady`) arrives while a weaker chain candidate (e.g. `building`) has partial (1–2 opportunity) pending progress will now show that weaker state as confirmed slightly earlier than before, instead of silently staying at `insufficient_data`/the prior confirmed value until the stronger state independently completes its own 3-in-a-row. This is strictly an *improvement* in information shown (never a regression per the exhaustive proof above) and never changes the timing of a transition that was already going to happen via 3 identical consecutive candidates (Rule 1, unchanged) — including both locked `perfect_completion_history` checkpoints, reproduced exactly.

**Edge Function impact: none**, for the same reason as fix 2 — `momentum.ts` is not in `scripts/build-edge-functions.js`'s `SOURCES` whitelist.

**Full verification.** `npm test` — 10 suites, 159 tests, all passing (8 new in `momentum.exhaustive.test.ts`). `npx tsc --noEmit` — clean. `npm run lint` — clean.

## Post-completion manual acceptance fixes (pre-Phase-5)

A targeted correctness pass over Progress/history issues found during manual on-device testing, requested before Phase 5 (AI Coach) begins consuming these behavioural outputs. Not a redesign; Phase 5 has still not begun.

### Item 0 — precondition

The approved current-day Momentum semantics (`docs/phase-2-implementation-plan.md` §3: "a miss is a Scheduled Opportunity for `date < today` with no qualifying Completion — today is never classified as missed") **remain unimplemented**. `lib/domain/momentum.ts`'s `recordsUpTo` builds its rate-window records from `scheduledOpportunitiesUpTo`, which includes today, and calls `isDoneOnDay` for it — an unlogged today reads as `completed: false` in every rate/quiet/building/rebuilding window, identically to a genuine miss. This was already flagged, and explicitly deferred, in "Post-completion fix 2" above (see the "Separately flagged, not fixed" paragraph). It is confirmed still unimplemented as of this pass. Per instruction, it was **not** implemented here.

Item 3's Recovery display fix (below) is independent of this defect — it corrects a presentation-layer bug (Progress borrowing Momentum's confirmed state as a readiness proxy for an unrelated metric) that exists regardless of whether Momentum's current-day semantics are ever fixed. Item 2's proposed narrative override has intentionally **not** been implemented, specifically because presentation logic should not be built on top of a domain calculation already known to be incorrect (see below).

### Item 1 — Progress "stale update": not a rendering defect

No store-subscription bug was found. `habit-store.tsx`'s reducer returns a new `state` object on every mutation; the memoized `store` object depends on `[state, hydrated]`; `progress.tsx` reads `useHabitStore()` with no local memoization and recomputes every derived value fresh on every render; the tab navigator uses default React Navigation behaviour (`enableFreeze` is never called), so no screen is frozen while off-screen. **What causes recomputation now:** any state change produces a new context value, and Progress re-renders from it on the very next render — there is nothing to fix here.

The perceived staleness is instead a downstream visual symptom of two independent, already-classified issues: item 2's confirmed-state hysteresis genuinely not updating for a day or more after a real change (by design — see Momentum contracts in `CLAUDE.md`), and item 3's presentation gate (below) hiding data that was, in fact, already fresh. Classified as domain-result / presentation-result symptoms, not stale-render.

### Item 2 — Quiet stretch semantics: deferred pending the current-day fix

**Investigation** (fully described in the prior turn, reproduced here for the record): a constructed history — 7 days completed, 3 missed (confirms `quiet`), then 2 genuine recoveries, with the 14th day ("today") left open — shows `candidateStateAt` at today reading `building` while `confirmedStateAt` remains `quiet`, and `openLapse(...)` at today is `null` (no unresolved miss run through yesterday). The badge and narrative both read as if the habit currently needs a return, when it does not. Root cause: today's own unlogged status breaks what would otherwise be a 3rd consecutive `rebuilding` candidate (days 12–14), which is item 0's defect manifesting at the confirmed level.

**Decision: the proposed narrative override is not implemented.** Building presentation logic that reads `openLapse`/today-completed signals to override the `quiet` narrative would be built directly on top of a candidate sequence that item 0 already established is generated from incorrect current-day handling. The override's condition (`openLapse === null`) would frequently be true for exactly the wrong reason (a real defect suppressing genuine miss-run detection into a benign-looking `building`/other candidate), not because the history is genuinely healthy.

**The proposed mapping is recorded here as a candidate solution only, not implemented, pending re-evaluation after the current-day fix** (see the follow-up proposal below, which found the specific scenario above no longer produces a `quiet` confirmed state at all once current-day semantics are corrected — suggesting the override may turn out to be unnecessary):

- Today already completed → acknowledge today's completion instead of urging a return.
- Today still open and `openLapse === null` → forward-looking, non-generic copy instead of "Today is a good day to return."
- Today still open and `openLapse !== null` (genuine unresolved miss run) → unchanged, existing copy is correct.

**Two problems with the specific copy proposed, recorded so they aren't re-proposed unchanged:**

1. `"You kept it going today — that's what counts."` contains an em dash, which this project's copy rules (see the new "User-facing copy" section in `CLAUDE.md`) now explicitly prohibit in any user-facing string.
2. `"Recent days have gone well — today's opportunity is still open."` (besides the same em-dash problem) overclaims what `openLapse === null` actually establishes. `openLapse` only judges through yesterday, so `null` means only "no unresolved miss run through yesterday" — it says nothing about days further back. In the exact day-14 example used to justify this branch, the same habit had a genuine 3-day miss four days earlier (days 9–11). Calling that "recent days have gone well" overstates the evidence the branch condition actually supports.

No wording change was made to `app/(tabs)/progress.tsx`'s `MOMENTUM_COPY` or `HabitSnapshot`'s narrative rendering in this pass.

### Item 3 — Writing Recovery discrepancy: fixed (presentation layer)

**Investigation:** a constructed 6-completion, alternating complete/miss history (a plausible real "gaps and returns" pattern) produces `recoverableLapseInstances`: 5 resolved, all recovered; `recoveryRate.rolling`: `{ resolvedCount: 5, recoveredCount: 5, rate: 1, displayAsPercentage: true }`; `averageRecoveryTime`: 1 day — genuinely ready to display a 100% recovery rate per Recovery Rate's own domain rule (`RECOVERY_CONFIG.minResolvedLapsesForPercentage = 3`, already met at 5). But the momentum candidate sequence for the same history oscillates `recovering ↔ quiet` every single day and never accumulates 3 identical trailing candidates, and since both are off-chain (absent from `EVIDENCE_CHAIN`), the confirmed-state raise rule never applies either — **confirmed Momentum State is permanently trapped at `insufficient_data`** for this history (see the "Trapped confirmed state" section below).

**The bug:** `app/(tabs)/progress.tsx`'s `HabitSnapshot` gated the entire Recovery Rate/Recovery Count/Average Recovery Time block behind `isNew = confirmedStateAt(...) === 'insufficient_data'`, showing "Come back after a few more days to see recovery and consistency here." instead whenever Momentum's confirmed state was `insufficient_data` — regardless of what Recovery Rate's own domain output actually had to say. **This is an architectural defect, not merely a UI inconvenience: it violates this project's core principle (`CLAUDE.md`, "Shared domain layer") that no screen may redefine or gate a behavioural concept using a rule that concept's own domain layer doesn't define.** Recovery Rate already owns its complete, correct readiness rule — `recoveryRateText` (same file) already reads `RecoveryRateResult.resolvedCount`/`displayAsPercentage` and degrades gracefully (`"Not enough recovery history yet"` when `resolvedCount === 0`; `"Recovery history still building"` when below the percentage threshold). The outer `momentumState === 'insufficient_data'` gate was a second, competing readiness rule for the same concept, invented in the screen and sourced from an unrelated domain output (Momentum State), which is exactly the "no screen may reimplement a concept that lives here" violation the domain layer's design is meant to prevent.

**The fix:** removed the `isNew` gate and its placeholder branch from `HabitSnapshot` entirely (`app/(tabs)/progress.tsx`). The Recovery Rate block now always renders, letting `recoveryRateText`/`avgRecoveryDays !== null`/`recoveryCount > 0` — Recovery Rate's own, already-correct domain output — decide what to show. No change to `lib/domain/recovery.ts` or `RECOVERY_CONFIG`. For the alternating-history case above, this now correctly surfaces "Recovery rate: 100% · 5 recoveries" and "Averages 1.0 day to bounce back" regardless of Momentum's confirmed state.

**Other occurrences of the same coupling — reported, not changed.** A grep of `app/(tabs)/progress.tsx` and `app/habit/[id].tsx` for every place a Momentum state gates a non-Momentum concept found exactly one more instance:

- `app/(tabs)/progress.tsx`, in the per-habit list (`ProgressScreen`'s render, ~line 299): `const isNew = confirmedStateAt(habit, schedulePeriods, logs, today) === 'insufficient_data'; const habitConsistencyText = consistencyText(habit, logs, days, isNew);` — Momentum's confirmed state chooses whether the below-heatmap Consistency line shows a raw count (`"2 of 7 days"`) or a percentage. `consistency()` (`lib/domain/habit-stats.ts`) has no "insufficient data" concept of its own — it's a plain fraction over a fixed window regardless of habit age — so this is the same pattern: Momentum State standing in as a readiness proxy for a metric that doesn't define that concept itself. Because this habit-list `isNew` also gets recomputed from the trapped-state history above, a habit like the Writing example would show a raw count instead of a consistency percentage indefinitely, for the same underlying reason as the Recovery bug.
- `app/habit/[id].tsx` has no Momentum import or usage at all — no coupling found there.

Per instruction, this second occurrence was **not** changed in this pass — listed here for separate review.

### Current-day Momentum semantics: confirmed defect, follow-up proposal (not implemented)

A local, uncommitted scratch change was made to `lib/domain/momentum.ts`'s `recordsUpTo` — excluding the record for `asOfDate` itself from the window when unlogged, rather than counting it as `completed: false` — to regenerate the Item 2 and Item 3 traces under corrected current-day semantics, then fully reverted (`git checkout -- lib/domain/momentum.ts`; confirmed clean via `git diff --stat`). **This diff was never committed and is not part of this pass's pushed changes.**

**New sequences under the scratch fix:**

Item 2's day-14 history (7 completed, 3 missed, 2 recovered, day 14 open):

| date | candidate (before fix) | confirmed (before) | candidate (after fix) | confirmed (after) |
|---|---|---|---|---|
| d9 | quiet | steady | building | steady |
| d10 | quiet | steady | quiet | steady |
| d11 | quiet | quiet | quiet | steady |
| d12 | rebuilding | quiet | rebuilding | steady |
| d13 | rebuilding | quiet | rebuilding | steady |
| d14 (today, open) | building | **quiet** | rebuilding | **rebuilding** |

Under corrected semantics, this history **never confirms `quiet` at all** — confirmed stays `steady` throughout the decline, then jumps directly to `rebuilding` once three consecutive `rebuilding` candidates (d12–d14) hold. This directly suggests the Item 2 narrative override may not even be needed for this class of scenario once the real fix lands — reinforcing the decision to defer it rather than build it against the current, known-incorrect candidates.

Item 3's alternating 6-completion history:

| date | candidate (before) | confirmed (before) | candidate (after) | confirmed (after) |
|---|---|---|---|---|
| d1–d2 | insufficient_data | insufficient_data | insufficient_data | insufficient_data |
| d3 | recovering | insufficient_data | recovering | insufficient_data |
| d4 | quiet | insufficient_data | recovering | insufficient_data |
| d5 | recovering | insufficient_data | recovering | **recovering** |
| d6–d11 | quiet/recovering (alternating) | insufficient_data (trapped) | recovering | recovering |

Under corrected semantics, the oscillation stops: `quiet` never triggers on an open "today" evaluation (once today's own unlogged record is excluded from its own window, the window's last visible entry is always an already-resolved past record, which for this alternating pattern is always a completion — failing `isCurrentlyQuiet`'s own-window-must-end-in-a-miss gate). Candidates converge to a stable `recovering` by day 5 and confirm there, never reaching `insufficient_data`'s trap. **This directly answers "is the `quiet` narrative override or the Recovery gate fix still needed after the domain fix": for this specific history, both symptoms disappear at the domain level once current-day semantics are corrected** — though Item 3's presentation fix remains independently correct regardless (a screen should never gate one domain concept's display on another's output, whether or not Momentum happens to be trapped).

**Critical finding — the naive fix is not safe to ship as-is.** Re-running the full suite under the scratch change found three regressions, not zero:

1. `momentum.test.ts`'s locked hysteresis test ("requires the new candidate to hold for 3 opportunities before the confirmed state changes") fails: expected `quiet`, got `steady` — the decline-to-`quiet` transition timing shifts by at least one opportunity, because excluding "today" from its own window during a live walk-forward reconstruction (`confirmedStateAt` calls `candidateStateAt` once per historical date, treating each in turn as if it were "today") delays how quickly a miss run's evidence counts in every affected day's own candidate reading, not only the literal current day's.
2. `momentum.exhaustive.test.ts`'s today-open-vs-completed monotonicity sweep, previously **0 violations**, now finds **3 new confirmed-level violations** (e.g. prefix `[1,1,1,0]`: leaving day 4 open reads confirmed `building`, completing it instead reads confirmed `insufficient_data` — strictly worse, the exact class of regression Post-completion fix 3 above was written to eliminate).
3. The 7-state reachability witness for `rebuilding` changes from the locked `'0000111'` to `'0000110'` — still reachable, but at a different history, breaking the exact-witness regression lock.

**Root cause of the naive fix's regressions:** `candidateStateAt(habit, periods, logs, asOfDate)` has no way to distinguish "the literal, real, live current day" from "a historical date being walked as if it were today" — `confirmedStateAt` calls it once per historical Scheduled Opportunity, and applying "exclude self if unlogged" uniformly at every one of those calls over-applies the "today is never classified as missed" rule to every day in the reconstruction, not just the one day it was written for.

**Recommendation for the actual follow-up implementation:** thread the real, live `today` (as received by `confirmedStateAt`) through to `candidateStateAt` as a value distinct from `asOfDate`, and apply the exclude-if-unlogged rule only when `asOfDate === today` (i.e., only for the final, live evaluation point of the walk — every earlier historical date is, by the time it's being scanned, a fully-resolved past fact and should keep today's existing "unlogged = miss" treatment). This is a `candidateStateAt`/`confirmedStateAt` signature change, not a one-line fix, and needs its own exhaustive monotonicity/reachability re-verification (the existing suites in `momentum.exhaustive.test.ts` are exactly the right tool for that) before it can be proposed for approval. Not implemented in this pass, per instruction.

### Current-day Momentum semantics, revised: threading `today` correctly resolves the naive fix's own regressions

A second scratch pass (uncommitted; reverted via `git checkout -- lib/domain/momentum.ts`, confirmed clean) implemented the recommendation above precisely: `today` threaded through `candidateStateAt`/`momentum` as a parameter distinct from `asOfDate`, `confirmedStateAt`'s internal walk passing its own `today` unchanged to every call rather than each walk step's own date.

- **The locked hysteresis fixture now passes unmodified** — the naive fix's regression is specifically resolved by anchoring exclusion to the one real `today` rather than to whichever date a given sub-computation happens to be looking at.
- **The 7-state reachability witness is not actually broken** — both the original locked witness (`'0000111'`) and the newly-found one (`'0000110'`, today left open) independently confirm `rebuilding`; the search just finds the numerically smaller mask first. Test-authoring artifact (the assertion pins "whichever the search finds first," not "is this state reachable"), not a behavioural regression. The witness value needs relaxing, not the design.
- **The three monotonicity violations survive, but not for the reason first assumed.** Re-examined at the mechanism level (not just the output level): shipped code reaches equality on these histories (0 violations) because *both* comparison sides are independently blocked by the same veto — the open side by `quiet` (an off-chain candidate produced by the current-day miscount), the completed side by `recovering` (an off-chain candidate produced by `isRecentShortRecovery`'s own, unrelated vacuous-true check when nothing follows a same-day recovery yet). Both routes converge on the identical mechanism: `computeConfirmedMomentumState`'s Rule 2 is unconditionally disabled the instant *any* window member is off-chain, regardless of how strong the rest of the window is. Fixing the current-day miscount removes the open side's route into that veto (its candidate becomes `building`, on-chain, passing Rule 1 cleanly) without touching the completed side's route (which was never about current-day counting at all). The result: this is **one shared structural defect reached through two independent entry points, whose simultaneous presence on this specific comparison is what made shipped code's proof pass** — not two unrelated bugs that happened to cancel, and not one defect masquerading as two. The monotonicity check was only ever validating that both entry points agreed; correcting one exposes that the other was never actually fixed.

**Conclusion: the current-day design itself (scheduling-fact vs. evidence-judgment split, below) is settled and correct, but cannot ship while the `recovering`/Rule 2 interaction it exposes remains open.** That interaction is a confirmation-mechanism question, not a current-day-semantics question, and is tracked as its own blocker below.

### Current-day design, settled: scheduling fact vs. evidence judgment

The design converged on a real refinement beyond "exclude today's record from the window": `records.length` (and every `window.length` derived from it) answers a **scheduling** question — how many Scheduled Opportunities exist to evaluate — and today counts toward that the instant it's scheduled, resolved or not. `completionRate` and every predicate that inspects individual `.completed` values to detect a pattern (`isCurrentlyQuiet`'s last-item/fraction checks, `meetsRateWindow`'s `requireNoOpenLapse`/`requireNoLapseAtAll`, `isRebuilding`'s last-item check, `isRecentShortRecovery`'s `afterRecovery.every`) answer an **evidence** question — what does the record actually show — and only this side should ever be affected by today being unresolved. The spec's "today is never classified as missed" is about the evidence side only; it says nothing about sufficiency.

Settled shape: `recordsUpTo` stops filtering entirely (full array, today included regardless of completion status, so every `.length`/sufficiency/window-size check reaches full size on the same calendar day as shipped code — no delay). Six functions (`completionRate`, `isCurrentlyQuiet`, `meetsRateWindow`, `meetsBuilding`, `isRebuilding`, `isRecentShortRecovery`) each gain `today` and derive a resolved view — excluding only the pending (today, unlogged) record — for their own judgment, rather than one global filter in `recordsUpTo`. `OpportunityRecord`'s shape is unchanged (`{ date, completed }`); "pending" is derived inline (`date === today && !completed`), never stored. This is broader surface area than the original single-filter idea but is coherent, eliminates the one-day sufficiency delay the naive/first-corrected designs both had, and stays local to `lib/domain/momentum.ts` — `records.length` itself is unaffected, so nothing outside this file needs to change. Not implemented; this is the settled target shape for whenever the blocker below is resolved.

### Confirmation-mechanism blocker: `recovering`/Rule 2, investigated, not resolved

Checked the obvious fix — letting positive-family off-chain candidates (`recovering`, `rebuilding`; both require the window's final opportunity to be a completion by their own definition) stop blocking Rule 2 — against the rebuilding-precedence fix (Post-completion fix 2) and the three-opportunity evidence requirement (the load-bearing invariant underlying Post-completion fix 3, reading (a), and reading (b), each independently rejected on the same grounds).

**First construction rejected on inspection.** "Non-blocking, evaluate the surviving chain-comparable members" fails concretely: window `[rebuilding, rebuilding, steady]` (day 3's own candidate independently clears `steady`'s bar, which already takes precedence over `rebuilding` in `candidateStateAt`'s evaluation order — an existing, unrelated behaviour) has exactly one chain-comparable member. Evaluating Rule 2 over just that member raises confirmed straight to `steady`, justified by one real opportunity of evidence, not three. Rejected without building it, on the same grounds as the naive current-day fix's own violations.

**Second construction — "no assigned position, min over chain-comparable members only, all three slots still counted as present" — demonstrated (not merely asserted) not to work.** It resolves `[building, building, recovering]` correctly (min of the two `building` candidates raises confirmed to `building`), but fails identically to the first construction on `[rebuilding, rebuilding, steady]` (min over the single surviving chain-comparable member is still `steady`). The reason is structural, not case-specific: computing `min` over a subset that excludes the off-chain members can never produce a value lower than including them would, for any value they might otherwise contribute — exclusion from a minimum is not neutral, it is inflationary, regardless of how many members remain. "Affirmative evidence without an assigned position" is incoherent as an input to a minimum-based rule specifically: `min` requires either a comparable value for everything it aggregates over, or exclusion, and exclusion is a count reduction under a different name. This is the fourth instance of the same load-bearing requirement being weakened in a non-obvious way (reading (a): weakened directly; reading (b): weakened by consuming rungs; the transparency/first construction: weakened by removing members from the count; this second construction: weakened by excluding members from the aggregate despite claiming to retain them).

**A value-bearing fix was derived, and is parked, not proposed.** Solving for the one value that resolves both test cases: `min(building, building, X) = building` requires `X <= building`'s rank; `min(rebuilding-as-X, rebuilding-as-X, steady) != steady` requires `X < steady`'s rank. The chain's weakest rung (`building`) is the unique value satisfying both. Framed as a **floor**, not a rank: a positive-family off-chain candidate is affirmative evidence sufficient to support the chain's weakest rung and no more — never compared to `quiet`, never compared to `steady`/`thriving`, never exposed as a general, reusable rank the way `EVIDENCE_RANK` is. Whether this floor is admissible under the "no ordering between off-chain states and the chain" constraint is **not decided in this document** — it is an open product decision (see below), not a technical conclusion. Neither this construction nor the rejected ones have been built or swept (no exhaustive monotonicity/reachability/rebuilding-witness verification performed); none is proposed for implementation.

**Status: resolved.** See "Confirmation-mechanism blocker resolved" immediately below. Phase 5 remains not started.

### Confirmation-mechanism blocker resolved: positive-family evidence floor implemented

**Ruling (explicit, this pass).** The positive-family evidence floor is admissible in principle: a uniform floor at the chain's weakest rung orders nothing — it draws no distinction between `recovering` and `rebuilding`, says nothing about either relative to any chain state beyond the single fixed floor value, and leaves `quiet` untouched. It is a statement that closing a lapse is affirmative evidence sufficient to support the chain's weakest rung and no more, scoped to Rule 2's minimum calculation and never exposed as a general, reusable rank. Approved as the product interpretation, conditional on the same exhaustive verification given to every other confirmation-mechanism change in this document.

**Implementation.** `lib/domain/momentum.ts` gains `rule2EvidenceRank(state)`, a narrower rank used only inside `computeConfirmedMomentumState`'s Rule 2 branch: `EVIDENCE_RANK[state]` unchanged for the four chain states, `EVIDENCE_RANK.building` for `recovering`/`rebuilding`, `undefined` for `quiet` (and for `insufficient_data`/`building`/`steady`/`thriving` it's simply a pass-through — no behaviour change for pure chain windows). Rule 2's window-comparability check and minimum calculation now call `rule2EvidenceRank` instead of `EVIDENCE_RANK` directly; nothing else changed. Rule 1 (exact-match) is untouched. `EVIDENCE_RANK`/`EVIDENCE_CHAIN` themselves are untouched and not exported with `recovering`/`rebuilding` added — the floor is not merged into the general rank, exactly as the ruling requires. `confirmedRank` (whether the *current* confirmed state is chain-comparable) still reads `EVIDENCE_RANK[confirmed]` directly, never the floor — an off-chain confirmed state (`quiet`/`recovering`/`rebuilding`) can still only ever be left via Rule 1, unchanged.

**Exhaustive verification, performed before committing, per the two motivating cases from the "First"/"Second construction" writeups above:**

- `computeConfirmedMomentumState(['building', 'building', 'recovering'])` → `'building'` (previously blocked entirely, retained `insufficient_data`).
- `computeConfirmedMomentumState(['rebuilding', 'rebuilding', 'steady'])` → `'building'`, **not** `'steady'` — the specific failure mode both rejected constructions above produced (raising off one real opportunity of chain evidence). The floor resolves this correctly: `rebuilding`'s floor rank (`building`'s) is the window's minimum, so the raise is capped there.
- `quiet` confirmed untouched: `['thriving', 'thriving', 'quiet']` and `['quiet', 'recovering', 'quiet']` both still retain (`insufficient_data`, in isolation) — `quiet` has no floor entry, so it still fails `window.every(...)` and blocks Rule 2 outright whenever present, exactly as before.
- An off-chain **confirmed** state can still only be left via Rule 1: driving confirmed to `quiet` via 3 identical candidates, then presenting `[rebuilding, rebuilding, steady]` (a window that would raise a chain-confirmed state) leaves confirmed at `quiet` — `EVIDENCE_RANK[confirmed]` is undefined for `quiet` regardless of the floor, so Rule 2 stays gated off.
- **New exhaustive sweep** (`lib/domain/momentum.exhaustive.test.ts`, "positive-family evidence floor" describe block): of all 343 (7³) 3-candidate windows drawn from every `MomentumStateKey` value, 218 contain `recovering` or `rebuilding`; of those, 216 aren't a Rule-1 exact match. All 216 were fed through `computeConfirmedMomentumState`; the result is never chain-ranked above `building`. Proves the floor's bound (`Math.min` can never exceed the floor value once any window member contributes it) exhaustively rather than by construction alone.
- **Re-ran the existing 12-day today-open-vs-completed monotonicity sweep, unmodified:** 0 violations at both candidate and confirmed level, identical to before the floor.
- **Re-ran the existing 7-state reachability witness search, unmodified:** every state still reachable; `rebuilding`'s shortest witness is still the 7-day `'0000111'` pattern, byte-identical to before. This directly answers the specific concern raised when approving this work: the floor cannot reopen the rebuilding-precedence bug (Post-completion fix 2), because `rebuilding` is only ever produced as a confirmed value by Rule 1 (`EVIDENCE_CHAIN`, the set of values Rule 2 can output, is unchanged and still excludes `recovering`/`rebuilding`/`quiet` entirely) — the floor changes what a window containing `recovering`/`rebuilding` can raise a *chain* state to, never what `recovering`/`rebuilding` themselves require to confirm.
- **`perfect_completion_history` locked fixture:** unchanged — `steady` at opportunity 7, `thriving` at opportunity 10, exactly as locked. No chain-only window's evaluation changed at all (`rule2EvidenceRank` is a pure pass-through for the four chain states).

**One pre-existing characterization test's expected value changed, as a direct and foreseeable consequence of the approved floor — not a silent adjustment.** `momentum.exhaustive.test.ts`'s `['building', 'recovering', 'building']` case previously asserted `'insufficient_data'`, documented as "an off-chain state interrupting a run breaks it, no bridging." Under the floor, `recovering` is no longer an interruption — it's affirmative evidence at least as strong as `building`'s own bar — so all three window members resolve to `building`'s rank and confirmed correctly raises to `'building'`. Updated in place with a comment explaining the change; not deleted, not loosened beyond what the ruling requires.

**Full verification.** `npm test` — 10 suites, 178 tests, all passing (5 new in `momentum.exhaustive.test.ts`: the two motivating cases, the `quiet`-untouched case, the off-chain-confirmed-only-leaves-via-Rule-1 case, and the 216-window exhaustive sweep — plus its one pre-existing case updated in place, described above). `npx tsc --noEmit` — clean. `npm run lint` — clean.

**Scope boundary, not addressed here.** This resolves the floor's own admissibility and correctness only. The separate current-day Momentum semantics fix (`recordsUpTo`'s "scheduling fact vs. evidence judgment" split, settled but not implemented — see above) still needs its own fresh verification pass once actually implemented, combined with this floor, before it can ship — resolving this blocker unblocks that path in principle (per the dependency recorded below) but does not itself re-verify it. Not undertaken in this pass; not requested.

### Trapped confirmed state: an open product decision, not possibly-correct behaviour

Reframed per explicit instruction: this is a defect, and `insufficient_data` is not an acceptable fallback description of the affected histories — it is not "possibly correct as-is," and this document no longer proposes that it might be.

A characterization test was added — `lib/domain/momentum.test.ts`, describe block `"known issue (documented, not fixed by this pass): confirmed state can become permanently trapped at insufficient_data"` — asserting the exact 11-opportunity alternating-completion sequence from Item 3 stays at confirmed `insufficient_data` for its entire length, and pinning the full candidate sequence (`insufficient_data, insufficient_data, recovering, quiet, recovering, quiet, recovering, quiet, recovering, quiet, recovering`). It documents current, observed behaviour; it is **not** a locked contract.

**This is a plausible real usage pattern, not merely a synthetic edge case** — any habit genuinely done every other day for an extended stretch (a common, deliberate cadence for some habits, not just an unlucky one) produces exactly this oscillation. With Item 3's presentation fix (above) now shipped, Progress displays that habit's real 100% rolling Recovery Rate and 5 Recovery Events on the same card as a Momentum badge reading "Getting started" / "Still gathering enough days to show a pattern" — a direct, visible, on-screen contradiction for exactly this history, not a cosmetic wording concern.

**Why the existing monotonicity and reachability proofs don't detect this.** `momentum.exhaustive.test.ts`'s today-open-vs-completed monotonicity sweep only ever compares two histories that differ in whether the *final* day is open or completed — it says nothing about a habit that alternates for its entire lifetime, so a permanently-oscillating sequence is outside its comparison space entirely. The 7-state reachability sweep only asks whether each state is reachable *at all* within a bounded history length — it has no notion of "does the confirmed state ever get stuck," so a state (`insufficient_data`) that is trivially reachable (it's the starting value) passing that check says nothing about whether it can also become *inescapable*. Neither proof was designed to detect a state that is reachable, never left, and permanently retained regardless of how much further evidence accumulates — a different property (liveness/eventual-escape) than either existing suite checks.

**The missing distinction, not a proposed fix:** all seven current `MomentumStateKey` values describe one of — not enough data yet; an improving trend; a high, stable rate; an anomaly being actively corrected (`recovering`/`rebuilding`, both built on a *lapse-and-return* narrative, a deviation from a different baseline); or an ongoing absence (`quiet`). None describes *sustained, repeated, moderate engagement that is not an anomaly* — a habit where alternating completion is the baseline, not a deviation from one. `recovering`/`rebuilding` are the closest existing states but are framed around correcting a departure, which misdescribes a pattern that recurs by its own nature.

**Not resolved here, and not to be resolved by implementation or a new behavioural rule.** Recorded as an open product decision blocking Phase 5 (see below), independent of the confirmation-mechanism blocker. A new eighth `MomentumStateKey` was subsequently investigated as one candidate resolution and rejected on architectural grounds — see "Rhythm: an eighth Momentum State investigated and rejected" immediately below; that option is no longer live. Noted for whenever a decision is made: the remaining option under consideration is a condition-aware narrative sentence on the existing `insufficient_data` state — sharing a presentation mechanism with Item 2's already-deferred narrative override. The confirmation-mechanism work above is shaped so it doesn't foreclose that route: the parked floor rule (above) only ever affects which *state* confirms, never how a *state's copy* is derived, so a future narrative-level resolution for this item remains available regardless of how the floor's admissibility is ultimately decided.

**Status: resolved 2026-08-18.** See "Trapped confirmed state: discriminator resolved" below (after the Rhythm section) for the adopted condition and what remains deferred.

### Rhythm: an eighth Momentum State investigated and rejected

A signal named **Rhythm** — sustained lapse-and-return as a habit's own established pattern, evidenced by a meaningful number of resolved lapse instances with a high recovery rate and low recovery time, where the returning is the baseline rather than a deviation from one — was investigated as a candidate resolution for the trapped-`insufficient_data` item above. **An eighth `MomentumStateKey` was the working assumption partway through the investigation and was reversed.** Rhythm will not become a Momentum State. The reasoning follows, in the order it was reached.

**1. Momentum States are opportunity-local; Rhythm's defining quantities are not.** `candidateStateAt` classifies a single opportunity from a small trailing window (0/3/5/8 Scheduled Opportunities, depending on the check). Rhythm's defining quantities — resolved lapse instance count, recovery rate, recovery time — fold over the habit's entire (or, for the rolling recovery-rate horizon, `RECOVERY_CONFIG.rollingWindowOpportunities`-many resolved-instance) lapse history, via `recoverableLapseInstances`/`recoveryRate`/`averageRecoveryTime` (`lib/domain/recovery.ts`). That these functions share `candidateStateAt`'s exact `(habit, periods, logs, today)` call shape, and are therefore callable from inside it without new plumbing, is an artefact of the domain layer's stateless-recomputation design — the same property that would make `totalCompletions` or `longestStreak` equally "reachable" from `candidateStateAt` without making either of them a candidate state. Reachability by call shape is not evidence of opportunity-local grain.

**2. Precedence makes the tension concrete.** `candidateStateAt`'s evaluation is first-match-wins, in order: `insufficient_data → recovering → quiet → thriving/steady → rebuilding → building`. A habit whose real pattern is exactly what Rhythm is meant to describe — frequent short lapses immediately followed by completion, repeated as baseline — satisfies `isRecentShortRecovery` (`recovering`) on its completion days and, depending on its trailing miss fraction, can satisfy `isCurrentlyQuiet` (`quiet`) on its miss days; both are checked ahead of any position later than second. So a Rhythm check would have to sit at or before `recovering` to ever fire for the habit type it targets. But Rhythm's condition is a slow-moving lifetime aggregate that, once true, stays true across many consecutive opportunities — unlike `recovering`/`quiet`, whose conditions reset day to day from a small trailing window. Placing the check early enough to be reachable does not make it occasionally visible alongside `recovering`/`quiet`; under first-match-wins it would claim essentially every subsequent opportunity for that habit from the day its aggregate first crosses threshold, permanently pre-empting both. Reachability and non-domination are in tension at every position in the current precedence order — not a threshold-tuning problem, a structural one.

**3. A third off-chain family reopens the paused exit-rule work's hardest case.** Rhythm is explicitly off-chain (measured on the recovery axis, not the completion-rate axis — see the chain definition in `docs/phase-2-implementation-plan.md` and this file's own Momentum contracts, mirrored in `CLAUDE.md`). Today, two off-chain families can appear together in a trailing window (`quiet`, `recovering`; `rebuilding` shares `recovering`'s "requires a recent completion" shape). Any plurality-based off-chain exit rule considered for the paused confirmation-mechanism work can resolve a 3-window drawn from two families — a window of 3 with 2 possible values is always a 2-1 split. A third, genuinely independent off-chain family makes a 1-1-1 window possible, which no plurality rule can resolve without a tie-break, and any tie-break that isn't pure symmetry risks becoming exactly the ordering-between-off-chain-states that work is required to avoid. Recorded as a constraint on that paused work, not resolved here: whatever mechanism eventually handles mixed off-chain windows must be able to handle a genuine three-way tie, not just a two-family alternation, if an off-chain state like Rhythm is ever introduced by some other route.

**The corrected architectural boundary.** The investigation's working assumption at one point was that "opportunity-local vs. history-level" was itself the domain layer's grain boundary. On inspection it isn't quite that: all seven existing Momentum States are opportunity-local by the definition above (`quiet`/`recovering`/`rebuilding` included — they read small trailing windows and change opportunity to opportunity, exactly as `building`/`steady` do; they differ from the chain states in *what evidence they read*, not in grain). The actual load-bearing distinction is narrower and already exists in code: **whether a behavioural classification passes through the Momentum confirmation mechanism.** Hysteresis exists in exactly one place — `computeConfirmedMomentumState`'s fold over `candidateStateAt`'s output — and nowhere else in the domain layer. Everything that doesn't pass through that fold is a single-pass, stateless computation, even though those computations operate at several different granularities.

`streakForHabit` (`lib/domain/habit-stats.ts`) is the proof that the boundary is about mechanism, not volatility or window size: it's a full-history backward walk, exactly as volatile as any candidate check (one miss zeros it), and yet it was deliberately left unconfirmed as of Phase 3 — shown as plain text on Habit Detail ("Current: 0 · Best: 21," never a warning colour), demoted off Today/Progress specifically because of that volatility. If the boundary were about volatility, streak would belong on the Momentum side; it doesn't, because it never passes through `computeConfirmedMomentumState`.

**Functions on each side.** Opportunity-local / confirmed: `candidateStateAt`'s constituent checks (`meetsBuilding`, `isCurrentlyQuiet`, `meetsRateWindow` for `thriving`/`steady`, `isRecentShortRecovery`, `isRebuilding`), `confirmedStateAt`, `computeConfirmedMomentumState`, and the internal `momentum()` trend signal (windowed and volatile, but explicitly never surfaced — "Only Momentum State is user-facing"). History-level / unconfirmed: `recoverableLapseInstances`, `recoveryEvents`, `closedLapses`, `averageRecoveryTime`, `recoveryRate` (`lib/domain/recovery.ts`); `totalCompletions`, `longestStreak` (`lib/domain/habit-stats.ts`).

Two functions straddle the boundary as drawn by grain alone, which is exactly why mechanism — not grain — is the correct dividing line: `streakForHabit`, above. And `consistency()` (`lib/domain/habit-stats.ts`), which operates at a third, currently unnamed granularity — a rolling window of 7/14/28 *calendar days*, not Scheduled Opportunities, smaller and more volatile than recovery's lifetime/10-resolved-instance window but coarser than momentum's 3/5/8-*opportunity* windows, and not consumed by `candidateStateAt` at all (momentum's own rate windows are computed independently via `recordsUpTo`/`lastN`/`completionRate`).

**Non-contradiction finding.** On-chain states' qualifying conditions (`requireNoLapseAtAll` for `thriving`, `requireNoOpenLapse` for `steady`) require zero misses in their trailing window; every `recoverableLapseInstance` requires a miss (`!records[i-1].completed`) to exist at all. So an on-chain Momentum State and Recovery statistics are, by construction, computed from disjoint-or-nested slices of the same opportunity sequence whenever an on-chain state actually holds — they cannot be describing the same events, and so cannot be logically contradictory, only differently timescoped.

Worked example: a habit with three 3-day lapses each resolved by the next opportunity (days 1-3 miss / day 4 hit; days 5-7 miss / day 8 hit; days 9-11 miss / day 12 hit), followed by 10 consecutive completions (days 13-22). At day 22: `confirmedStateAt` = `'thriving'` (three consecutive 8-opportunity trailing windows — days 13-20, 14-21, 15-22 — all completions, zero lapse) — badge "Thriving," narrative "This habit is running strong right now." Simultaneously, `recoveryRate(...).rolling` = 9 resolved instances, 3 recovered, rate ≈ 0.33, `displayAsPercentage: true` — "Recovery rate: 33% · 3 recoveries" — and `averageRecoveryTime` = 3.0 — "Averages 3.0 days to bounce back." A reader could easily find "Thriving" next to "33%" confusing, but nothing here is false: the last miss (day 11) falls entirely outside `thriving`'s qualifying window (day 13 onward) — two true statements about two non-overlapping slices of the same history.

**Correction (found during a later coverage investigation, not this pass): the claim below was checked directly against the code and is false.** It is struck, not deleted, so a future reader can see it was checked and found wrong, not merely reworded.

~~One case is a proof, not just a construction: `displayAsPercentage` requires `resolvedCount >= RECOVERY_CONFIG.minResolvedLapsesForPercentage` (3), and `recoverableLapseInstances` needs at least 4 lifetime Scheduled Opportunities to produce 3 resolved instances. `insufficient_data` (candidate or confirmed) requires the lifetime opportunity count to be under `MOMENTUM_CONFIG.insufficientData.minScheduledOpportunities` (3). Both trace back to the same `scheduledOpportunitiesUpTo` count, so a displayed Recovery Rate percentage and `insufficient_data` can never coexist for the same habit at the same moment — not a design choice, arithmetic.~~

**Counterexample, verified by direct execution against `lib/domain/momentum.ts` and `lib/domain/recovery.ts`:** the 4-opportunity history `MMMC` (missed, missed, missed, completed) confirms `insufficient_data`, while `recoveryRate(...).lifetime` for the same habit at the same moment returns `{ resolvedCount: 3, recoveredCount: 1, rate: 0.333, displayAsPercentage: true }`. A displayed Recovery Rate percentage and confirmed `insufficient_data` do coexist here.

**Root cause.** The struck claim conflated two different thresholds under the phrase "`insufficient_data` (candidate or confirmed)." `MOMENTUM_CONFIG.insufficientData.minScheduledOpportunities` (3) genuinely bounds *candidate*-level `insufficient_data` — `candidateStateAt` cannot return it once the lifetime opportunity count reaches 3. It does not bound *confirmed*-level `insufficient_data`, which is governed separately by the confirmation window in `computeConfirmedMomentumState` and can remain `insufficient_data` well beyond 3 opportunities, for reasons unrelated to raw opportunity count (see the coverage finding below). This is exactly the candidate-versus-confirmed conflation `CLAUDE.md`'s "Momentum contracts" section requires classifying separately ("A candidate-level issue indicates a contradiction in `candidateStateAt`. A confirmed-level-only issue is confined to the confirmation mechanism. Treat these as distinct classes of defect and report them separately.") — this correction is that rule catching a real error in this document's own reasoning, not a new discovery about the code's behaviour.

**Scope of the correction.** The **Non-contradiction finding** above and its worked `thriving`/33%-recovery-rate example are unaffected — that argument rests on disjoint-or-nested opportunity windows for *on-chain* states (`thriving`, `steady`, `building`), which never depended on the struck arithmetic. Only this specific sub-claim about `insufficient_data` was wrong.

**Coverage finding (recorded here; this is what prompted the correction above).** Confirmed `insufficient_data` has two structurally distinct shapes. At 4 or fewer lifetime Scheduled Opportunities, confirmed is `insufficient_data` unconditionally, for every possible completion history — genuinely too little data to judge. From 5 opportunities onward, confirmed can also remain `insufficient_data` indefinitely for a second, different reason: the same confirmation-mechanism trap this document already tracks as an open product decision (every trailing 3-candidate window contains `quiet`, blocking Rule 2, without ever holding 3 identical candidates to trigger Rule 1). This second shape provably always has at least one completion — sustaining it requires periodic `recovering` (or, for longer lapses, `rebuilding`) candidates, which only fire on an actual completion closing a lapse, to keep breaking up `quiet`; an unbroken run of misses instead escapes into confirmed `quiet` within a few opportunities rather than staying in this state. Any future presentation decision for the trapped-`insufficient_data` item below will need to rest on this distinction.

**Consequence for this documentation task.** Rhythm is not added to `MomentumStateKey`, `MOMENTUM_COPY`, or any locked fixture. How, or whether, Rhythm is ever surfaced to a user is undecided and out of scope here. The trapped-`insufficient_data` item's open product decision (below) is narrowed by this outcome — a new `MomentumStateKey` is no longer one of its live options — but is not otherwise resolved.

### Trapped confirmed state: discriminator resolved

**The remaining option named above — a condition-aware narrative override on the existing `insufficient_data` state — is the one adopted.** Rhythm having been closed off (above), this was the only live option left, per the item's own prior framing.

**Discriminator, verified exhaustively: 5 or more lifetime Scheduled Opportunities, with at least one completion among them.** This is exactly the boundary the "Coverage finding" above already established between the two structurally distinct shapes of confirmed `insufficient_data`: at 4 or fewer lifetime Scheduled Opportunities, confirmed `insufficient_data` is unconditional and genuinely means "too little data yet" — no override applies. From 5 opportunities onward with at least one completion, confirmed `insufficient_data` can only be the confirmation-mechanism trap (the Coverage finding's proof: sustaining the trap past 4 opportunities structurally requires periodic `recovering`/`rebuilding` candidates, which only fire on an actual completion — an unbroken run of misses instead escapes to confirmed `quiet` within a few opportunities) — the override applies. Below 5 opportunities, or at 5-plus with zero completions, the existing "Still gathering enough days to show a pattern" copy remains accurate and is not overridden.

**Sentences are drafted, not recorded in this document.** Tracked with the other pending narrative amendments (the `recovering`/`steady`/`rebuilding` punctuation batch above) outside this document, per the same copy-approval path — consistent with the project's copy work being deliberately paused until after testers see the app.

**Only implementation is deferred, to the post-tester copy pass.** The product decision itself — that an override exists, and its exact triggering condition — is settled. This item no longer blocks Phase 5.

### Open product decisions blocking Phase 5

Both decisions are now resolved:

1. ~~**Admissibility of the positive-family evidence floor.**~~ **Resolved.** Ruled admissible — see "Confirmation-mechanism blocker resolved" above for the ruling, the implementation, and its exhaustive verification (12-day monotonicity sweep, 7-state reachability witnesses including `rebuilding`'s unchanged, a dedicated 216-window exhaustive sweep of the floor's own bound). The three-history monotonicity violation this decision was blocking is now unblocked in principle; the current-day semantics fix it in turn gates still needs its own implementation and fresh verification pass before shipping (not undertaken here).
2. ~~**Treatment of sustained/moderate alternating engagement currently trapped at `insufficient_data`.**~~ **Resolved 2026-08-18.** See "Trapped confirmed state: discriminator resolved" immediately above — a condition-aware narrative override on `insufficient_data`, triggered at 5 or more lifetime Scheduled Opportunities with at least one completion, verified exhaustively against the Coverage finding's own two-shapes proof. Sentences are drafted; only implementation is deferred, to the post-tester copy pass.

Neither open product decision blocks Phase 5 any longer. This resolves a documentation precondition only — it is not approval to begin Phase 5, which remains a separate, unmade decision. Phase 5 has not begun.

### Item 4 — Habit History UX improvements: recorded, not implemented

Recorded in `docs/implementation-roadmap.md`'s "Product Polish" section (previous-month calendar navigation, Recent Activity trimmed to ~5 entries) rather than implemented — `components/habit-calendar.tsx` has no month-boundary or paging concept today, so browsing earlier months is a structural addition, not a trivial config change, and trimming the Recent Activity list in isolation isn't independently justified without the calendar becoming the primary view first.

### Item 5 — Notification reference notes: recorded, not implemented

Recorded in `docs/implementation-roadmap.md`'s "Phase 8 — Notifications and analytics" section: context-aware notification patterns grounded in Momentum State / open lapse / recovery / today-still-open / recent history / time-of-day, and the existing shame-framing avoid-list. `lib/notifications.ts` untouched.

### Verification

- `npm test` — 10 suites, 161 tests, all passing (2 new: the Item 3 characterization test in `momentum.test.ts`, plus its own sub-assertions).
- `npx tsc --noEmit` — clean, no output.
- `npm run lint` — clean, no warnings or errors.
- `npx expo export --platform web` — bundles cleanly (1288 modules), all 15 static routes present including `/progress` and `/(tabs)/progress`.
- **Verification method per issue:** Item 1 (no code change) — code-path inspection of `habit-store.tsx`'s reducer/memoization and `progress.tsx`'s render, confirmed no memoization gap exists. Items 2 and 3's investigations, and the current-day-fix follow-up traces, were verified by **actually executing** the real domain functions against constructed fixtures (not manual reasoning alone) — see the temporary investigation file used during this pass (deleted before commit; the permanent characterization test in `momentum.test.ts` retains the Item 3 trace as a regression-locked assertion). Item 3's fix was verified the same way, plus a full suite run. None of this was verified on a physical device or in an authenticated runtime in this pass — that verification is still required before considering these fixes fully accepted.

## Current-day Momentum semantics: implemented

The current-day Momentum fix recorded as settled-but-not-implemented in "Current-day design, settled: scheduling fact vs. evidence judgment" above is now implemented, per explicit instruction, now that the confirmation-mechanism blocker it was gated on is resolved. Not a redesign — the design (scheduling fact vs. evidence judgment, `today` threaded as a value distinct from `asOfDate`) is exactly as recorded there. Phase 5 has still not begun.

**Implementation, `lib/domain/momentum.ts`.** `recordsUpTo` is unchanged — it never filtered on completion status and still doesn't; every `.length`/window-size/sufficiency check continues to reach full size the instant a date is scheduled, regardless of completion, with no one-day delay. Two new private helpers: `isPending(record, today)` (`record.date === today && !record.completed`) and `resolvedView(records, today)` (filters out the pending record, if any). The six evidence-inspecting functions the settled design named — `completionRate`, `meetsRateWindow`, `meetsBuilding`, `isCurrentlyQuiet`, `isRebuilding`, `isRecentShortRecovery` — each gained a required `today` parameter and now judge `resolvedView(window, today)` instead of the raw window for every check that inspects individual `.completed` values (rate numerator/denominator, last-item checks, `requireNoOpenLapse`/`requireNoLapseAtAll`). Window-size/sufficiency checks (`window.length < cfg.window`) still read the raw window, unaffected. `closedLapses(...)` calls inside `isRebuilding`/`isRecentShortRecovery` still pass `asOfDate` (the walk position), not `today` — Recovery semantics are untouched, per scope. `candidateStateAt` gained a required `today` parameter, threaded to all six helpers; `momentum` (the internal trend signal) gained the same, since it calls `completionRate` directly. `confirmedStateAt`'s own signature is unchanged — internally its walk now calls `candidateStateAt(habit, periods, logs, date, today)` for every opportunity `date`, passing its own fixed `today` unchanged at every step, rather than letting each step be its own anchor (this is the exact defect the earlier "naive fix" scratch pass hit, per the recommendation recorded above). No default value for `today` anywhere; every call site (`app/(tabs)/progress.tsx`'s two `confirmedStateAt` calls needed no change — its signature didn't move — and every direct `candidateStateAt`/`momentum` call site across `momentum.test.ts`, `momentum.exhaustive.test.ts`, and `dev-simulation.test.ts`) now passes it explicitly.

**Verification, performed before committing:**

- **Window-size/sufficiency gates unaffected.** `recordsUpTo` was not touched; `insufficientData.minScheduledOpportunities` (3), `building`/`quiet`/`recovering` (3), `steady`/`rebuilding` (5), `thriving` (8) all still reach full size on the same calendar day as shipped code — structurally guaranteed, not just spot-checked, since the raw (unfiltered) window is still what every length check reads.
- **Locked fixtures pass unmodified.** `momentum.test.ts`'s "requires the new candidate to hold for 3 opportunities before the confirmed state changes" (the exact test the earlier naive scratch fix broke) passes as-is. `momentum.exhaustive.test.ts`'s `perfect_completion_history` fixture still confirms `steady` at opportunity 7 and `thriving` at opportunity 10, byte-identical to the locked values in `docs/phase-2-implementation-plan.md` §8.
- **Full monotonicity sweep: zero violations**, both today-open-vs-today-completed (the existing 12-day exhaustive sweep in `momentum.exhaustive.test.ts`, both candidate and confirmed level) and the positive-family-floor's own 216-window sweep — both re-run unmodified and still passing, meaning the `recovering`/Rule 2 interaction that blocked this fix from shipping earlier (see "revised: threading `today` correctly resolves the naive fix's own regressions" above) is confirmed resolved now that the floor is in place.
- **Seven-state reachability, witnesses re-recorded.** Every state's shortest witness **length** is unchanged from before this fix: `insufficient_data` 1, `quiet`/`building`/`recovering` 5, `rebuilding`/`steady` 7, `thriving` 10 — re-derived by running the same exhaustive witness search against the pre-fix code (via a temporary `git stash` of the fix, confirmed reverted afterward) and comparing directly. Only `rebuilding`'s specific witness *pattern* changed, from `'0000111'` (today completed) to `'0000110'` (today open) — same length, both independently valid (the mask search finds the numerically smaller value first); see `momentum.exhaustive.test.ts`'s updated comment for the mechanism. No other state's witness pattern changed at all.
- **Item 2 scenario, traced date by date.** Reconstructed the scenario's shape from its original description (7 days completed; a 4-miss lapse recovered once; today left open, one Scheduled Opportunity after the recovery) since the original scratch fixture was never committed. Before this fix: candidate sequence for the final 6 days reads `quiet, quiet, quiet, rebuilding, rebuilding, building` (today, open, reads `building` — its own unlogged status miscounted as a miss, breaking what would otherwise be a 3rd consecutive `rebuilding` candidate) and confirmed reaches `quiet` by the 2nd of those days and **stays `quiet` through today** — exactly the originally-diagnosed bug (badge "Quiet stretch," narrative "Today is a good day to return," shown to a user who has already recovered twice and simply hasn't logged today yet). After this fix: candidate sequence for the same 6 days reads `quiet, quiet, quiet, rebuilding, rebuilding, rebuilding` — three consecutive `rebuilding` candidates, today included, since today's own pending record no longer counts against `isRebuilding`'s "must currently be completing again" check — and **confirmed reaches `rebuilding` on today itself**. Badge: "Rebuilding." Narrative: "You're re-establishing this after some time away." This directly answers the open question from Item 2's original deferral: the proposed narrative override is confirmed unnecessary for this class of scenario, matching the earlier (pre-floor) scratch prediction — the domain-level fix alone produces an accurate badge and narrative with no presentation-layer override needed. Per scope, no override was implemented.
- **Alternating complete/miss history, traced the same way.** Re-ran `momentum.test.ts`'s existing 11-opportunity alternating-completion fixture (Item 3's trapped-state history) both before and after this fix. **The trap persists, exactly as expected — confirmed stays `insufficient_data` for the entire 11-opportunity trace, identical before and after.** This is expected, not a target: the trap is a confirmation-mechanism property (no 3 identical trailing candidates ever accumulate, on-chain or off), unrelated to current-day handling, and remains tracked as its own open product decision (below), untouched by this fix. One test-only artifact was found and is not a behavioural change: `momentum.test.ts`'s own characterization line computed a *second*, different trace — `candidateStateAt(h, [], logs, d, d)`, treating each day as its own live "today" rather than a fixed real anchor — and that trace's oscillation (`recovering ↔ quiet`) collapsed to a steady `recovering` after day 2, because each odd (unlogged) day's own record is now excluded from its own evidence judgment when evaluated as its own "today." This is a direct, correctly-predicted consequence of the fix on a per-day standalone query; it does not describe what `confirmedStateAt` actually computes (which always uses one fixed anchor), and the test's expected array was updated in place with an explanatory comment — not a regression.
- **The 8 `scenarioPattern` entries in `lib/domain/dev-simulation.ts` were re-derived empirically against the fixed domain functions, not adjusted.** All 8 still produce the state named on their button: `quietStretch` → confirmed `quiet`, `rebuilding` → confirmed `rebuilding`, `building` → confirmed `building`, `thriving` → confirmed `thriving` (the four `dev-simulation.test.ts` actually asserts against `confirmedStateAt`), unchanged before and after. `missYesterday`, `missTwoConsecutive`, `recoverToday`, `recoverAfterMultipleMisses` are asserted only via `openLapse`, which judges strictly through yesterday and is structurally untouched by a fix scoped to today's own record — unaffected by construction, confirmed unchanged. **No scenario pattern needed changing.**

**Production impact.** The only histories that can differ before vs. after this fix are ones where today is a Scheduled Opportunity that has not yet been logged — every other history is byte-for-byte identical, because `isPending` can only ever be true for `date === today && !completed`, and every date strictly before `today` in every walk step was already, and remains, treated as a fully-resolved past fact. Within that population:

- **Corrected behaviour.** Any habit whose evidence-window judgment was being dragged down by counting today's not-yet-logged status as a miss it hasn't actually committed yet — concretely and structurally: excluding a would-be-miss from `completionRate`'s numerator and denominator together can only leave a rate unchanged or raise it (removing a zero-contributing item from both sides of a fraction never lowers it); excluding it from `isCurrentlyQuiet`'s missed-fraction calculation can only leave that fraction unchanged or lower it (proven algebraically: for `m` misses among the other `n-1` window members, `m/(n-1) <= (m+1)/n` always, since `m <= n-1`); excluding it from a last-item check (`requireNoOpenLapse`, `isRebuilding`, `isCurrentlyQuiet`) falls back to the last already-resolved day instead of unconditionally reading as a miss. **Every one of these six functions can therefore only read today's own contribution as equally or more favorable than shipped code did — never less favorable** — so no history newly regresses to a worse-looking Momentum State under this fix; the change is one-directional by construction, not just by the spot-checks above (which additionally confirm it empirically for two concrete, previously-diagnosed scenarios).
- **Unchanged behaviour.** Every habit once today has already been logged (no pending record ever exists — the entire fix is a no-op for that habit for the rest of the day). Every date strictly before today, in every walk. The already-known trapped-`insufficient_data` behaviour for alternating-engagement histories (Item 3, below) — traced directly above, confirmed identical before and after; a pre-existing open product decision, not introduced, altered, or resolved by this fix. All 8 developer-simulation scenario buttons.
- **Unavoidable change introduced by the corrected semantics.** None identified beyond the corrected direction itself. The reachability search found no state whose shortest-witness *length* changed (only `rebuilding`'s specific witness pattern, a search-order artifact, not a behavioural one), and the exhaustive monotonicity re-sweep (0 violations) found no case where the fix makes any traced history's badge read worse than shipped code did.

**Test changes, each flagged individually before updating, neither a locked fixture:**

1. `momentum.test.ts`'s "known issue... permanently trapped at insufficient_data" test: the `candidateSequence` expected array changed from an oscillating `recovering`/`quiet` pattern to a steady `recovering` from day 3 onward. Reason: documented inline above and in the test's own updated comment — this line evaluates each day as its own live "today," a different query from the (unchanged) `confirmedSequence` assertion in the same test, which is what the trapped-state characterization is actually about.
2. `momentum.exhaustive.test.ts`'s reachability witness test: `witness.rebuilding` changed from `'0000111'` to `'0000110'`. Reason: documented inline above and in the test's own updated comment — same witness length, both patterns independently valid, a mask-search-order artifact.

**Full verification.** `npm test` — 10 suites, 178 tests, all passing (0 new tests added; 2 existing expected values updated in place, both flagged above). `npx tsc --noEmit` — clean. `npm run lint` — clean. `npx expo export --platform web` — bundles cleanly (1290 modules), all 15 static routes present. Working tree confirmed clean after commit.

**Scope respected.** Item 2's narrative override was not implemented (the trace above shows it isn't needed for the scenario that motivated it — re-evaluation, as scoped, not construction). The second Momentum-gated coupling in `progress.tsx` (Consistency's raw-count-vs-percentage `isNew` gate) was not touched. No Momentum threshold, candidate window, the three-opportunity confirmation requirement, the trailing-window rules, the positive-family floor, Recovery semantics, or schedule semantics were changed.

## Post-completion: em-dash mechanical audit, pending copy amendments

A mechanical grep-and-punctuation pass over every em dash in user-facing copy (`app/(tabs)/progress.tsx`, `app/habit/[id].tsx`, `app/habit-form.tsx`, `app/(tabs)/index.tsx`, `app/onboarding.tsx`, `components/recovery-card.tsx`, both Edge Functions' prompt strings) and the AI-output sanitizer that already covers the Edge Functions at runtime (`sanitizeContent()`, both `supabase/functions/*/index.ts`). Punctuation-only fixes (comma/colon/full stop, no rewording) were applied and verified (`npm test` 178/178, `tsc --noEmit` clean, `npm run lint` clean). Three items came out of that pass that are recorded here rather than actioned, per instruction.

### Em-dash coverage test: design recorded, not built

A future Jest test asserting no em dash appears in user-facing string/template literals. Refinements to the original proposal, recorded before building it:

1. **Discovery: glob over `app/` and `components/`, not a fixed file list.** A fixed list guards against a covered file silently *leaving* the check, but it equally lets a new file silently never *join* it — the likelier failure mode across several more phases of screen additions. The test should assert the discovered file list is non-empty (so a broken glob fails loudly rather than silently checking zero files) and should consider snapshotting the covered file count, so an unexpected drop (a file renamed out of the glob's pattern, a directory restructure) also fails instead of passing vacuously.
2. **Character-naming exemption: allowlist by exact match on the specific `STYLE_RULES` literal, not by "contains `(—)`".** A substring-based allowlist would let a real, unrelated em dash pass anywhere inside a string that happens to also contain the parenthesised form elsewhere. Matching the exact literal closes that gap.
3. **Rule 1 of the copy variation contract (`CLAUDE.md`) is equally greppable, and the same infrastructure would cover it.** The prohibited-framings list (protecting, breaking, losing, keeping alive, getting back on track, streak language, absence day-counts, "don't") is enforceable by grep exactly as the em-dash rule is. `app/(tabs)/index.tsx`'s challenge banner ("keep your run alive" — see below) is direct evidence that manual review alone does not catch these; a shared test covering both checks is worth building together rather than separately.

Not built in this pass.

### Pending amendments, batched, not applied

- **`recovering`'s narrative punctuation** (`app/(tabs)/progress.tsx`, `"You're finding your way back — that's the part that counts."`) is left unchanged because it matches the locked wording in `docs/phase-3-experience-plan.md`'s §7.1 table exactly — a punctuation swap here is a spec amendment, not a mechanical fix, and needs the same approval path as any other locked-copy change.
- It will be batched into one amendment together with the `steady` and `rebuilding` narrative changes already pending (tracked outside this document).
- **A narrow exemption to the em-dash rule in `CLAUDE.md`**, for text that names the em dash character in order to prohibit it (`supabase/functions/*/index.ts`'s `STYLE_RULES` string, which must contain a literal `(—)` for the instruction to Claude to be legible). `CLAUDE.md` was not edited.

### Additional copy findings flagged during the audit, not fixed

Surfaced while fixing the em-dash punctuation on the same lines; the punctuation fix was applied, the flagged content was not touched.

- **`app/(tabs)/index.tsx`'s challenge banner** ("...log them all today to keep your run alive") uses "keep... alive," a prohibited loss-framing under Rule 1 of the copy variation contract (`CLAUDE.md`). The em-dash fix on this line does not address it.
- **`app/(tabs)/progress.tsx`'s "No data yet" empty state and `app/onboarding.tsx`'s "Track your progress" feature row** both explicitly contrast the product against streaks ("not just whether you kept a streak, but..."). Not prohibited by any existing rule, but flagged for a product decision on whether streak-contrast belongs on Progress at all.

## Post-completion fix 4: second and final Momentum-gates-a-non-Momentum-concept instance resolved

**The instance.** `app/(tabs)/progress.tsx`'s per-habit list (`isNew = confirmedStateAt(...) === 'insufficient_data'`, feeding `consistencyText`'s `showRawCount` parameter) used confirmed Momentum State to choose between showing Consistency as a raw count ("X of Y days") or a percentage. This was the second occurrence of the same coupling Item 3 fixed for Recovery Rate — a screen using one behavioural concept's readiness as a proxy for a different, unrelated concept's display rule.

**Investigation findings, ahead of the fix:**
- Consistency owns no readiness rule of its own anywhere in `lib/domain/` — `consistency()` returns a bare fraction, with no `displayAsPercentage`-style field and no `CONSISTENCY_CONFIG` analogous to `RECOVERY_CONFIG`. Unlike Recovery Rate, there was nothing for the screen to defer to.
- The raw-count-vs-percentage split was cosmetic, not behavioural. `recentHistory`'s window is always the full fixed `days` (7 or 14), never a smaller or variable denominator — so `X of Y days` and `round(X/Y × 100)%` are two renderings of the identical, always-fully-populated fraction. There is no small-sample volatility here the way a 1-of-1 Recovery Rate swings between 0% and 100%; a percentage is never statistically thinner than the raw count it's derived from. No readiness rule was warranted, so none was defined.
- Both the aggregate summary at the top of Progress and the Consistency tile in `app/habit/[id].tsx` already render Consistency as an unconditional percentage with no gate at all — the per-habit list was the only place in the app where Consistency's presentation varied.

**Fix.** Removed the `isNew` local and its `confirmedStateAt` call at the per-habit list's render site; `consistencyText` lost its `showRawCount` parameter and raw-count branch entirely, now always formatting the percentage. `app/(tabs)/progress.tsx`'s other `confirmedStateAt` call (inside `HabitSnapshot`, driving the Momentum badge itself) is untouched — that is Momentum State governing its own display, not this pattern.

**This is a pure refactor, not a product decision.** No domain-layer change, no new config, no new behavioural rule. Deleting an invented gate and adopting the presentation already used everywhere else Consistency appears in the app required no judgment call beyond confirming (above) that the two presentations were equally truthful.

**Re-checked, per instruction: this is the second and final instance of the pattern.** `app/habit/[id].tsx` has no Momentum import or usage of any kind — reconfirmed directly (grep), not assumed from the prior pass. No other screen reads `confirmedStateAt`/`candidateStateAt`. With this fix, no non-Momentum concept anywhere in the app has its display gated by Momentum State.

**Open item escalated, not fixed: `recentHistory`/`consistency` have no creation-date floor.** `scheduledOpportunitiesUpTo` (`lib/domain/schedule.ts`) never generates a Scheduled Opportunity before a habit's `createdAt` — an explicit, deliberate floor, load-bearing enough that its absence was the root cause of "Post-completion manual testing fix" above. `recentHistory` (`lib/domain/habit-stats.ts`) has no equivalent: it always walks back the full `days` window regardless of when the habit was created, and `isDoneOnDay` reads every pre-creation day in that window as a miss (no logs can exist before the habit did). This was noted as an adjacent, out-of-scope observation during the investigation for this fix; per instruction it is escalated here rather than left as a footnote, because this fix changes its user-visible severity. Before this fix, a habit created 3 days ago with 3 completions read "3 of 14 days" — imprecise (the window includes 11 days that were never real opportunities) but not self-contradictory, since the reader could reasonably infer the habit hasn't existed that long. After this fix, the same habit reads "21%" — a number that states a rate over a window most of which never happened, which is not a thin statistic but a wrong one. This is the same class of defect `scheduledOpportunitiesUpTo`'s creation floor exists to prevent, in a different function that never received the equivalent fix. **Not fixed here, per instruction.** Whether Consistency should floor its window at creation (mirroring `scheduledOpportunitiesUpTo`), shorten the window for young habits, or suppress the stat until the window has fully elapsed is a product decision, recorded here as blocking nothing yet but needing resolution before the acceptance pass, since it changes what the on-screen numbers mean while testing.

**Full verification.** `npm test` — 10 suites, 178 tests, all passing (0 new tests; no domain function touched). `npx tsc --noEmit` — clean. `npm run lint` — clean. `npx expo export --platform web` — bundles cleanly (1290 modules), all 15 static routes present including `/progress` and `/(tabs)/progress`.

## Post-completion fix 5: `consistency()` becomes schedule-aware — a correction, not an amendment

**Classification, stated explicitly per instruction.** This is the same shape of defect as "Post-completion fix 2"'s rebuilding-precedence bug: an implementation that diverged from text the product specification had already approved, not a new product decision requiring fresh approval. `docs/habit-tracker-evolution-plan.md`'s "Core Product Principle: Scheduled Opportunities" section names Consistency first in an explicit list — "Every progress calculation throughout the application must be based on scheduled opportunities rather than calendar days. This includes: Consistency, Completion rate, Momentum, ..." — and `docs/phase-2-implementation-plan.md` §1 independently states the design consequence: "every Consistency/Momentum window naturally excludes pre-creation dates without any special-casing in those functions — they only ever iterate real Scheduled Opportunities." `consistency()`'s calendar-day denominator never matched either statement. The approval already existed; the code just hadn't been brought into line with it.

**The fix.** `consistency()` (`lib/domain/habit-stats.ts`) no longer calls `recentHistory()` internally. It now derives its denominator from a new `scheduledOpportunitiesInWindow(habit, periods, days, asOfDate)` (`lib/domain/schedule.ts`), the windowed counterpart to the existing `scheduledOpportunitiesUpTo`. Placed in `schedule.ts` rather than inline in `habit-stats.ts` because it's pure schedule logic with no dependency on `logs`, alongside `scheduledOpportunitiesUpTo`/`nextScheduledOpportunityAfter` — the same module, same shape. Deliberately **not** built by filtering a full `scheduledOpportunitiesUpTo` walk down to the window: that function is O(habit age), and Progress calls `consistency()` three times per habit per render (per-habit list, weekly aggregate, monthly aggregate), which would have made a small-window question cost proportional to how old the habit is. `scheduledOpportunitiesInWindow` instead walks only the requested window — O(days) — reusing `isScheduledOpportunity` per day, the same primitive `scheduledOpportunitiesUpTo` itself is built on, so no new counting logic was written, only a bounded-cost walk instead of an unbounded one.

`consistency()`'s signature gained a required `schedulePeriods: HabitSchedulePeriod[]` parameter, inserted after `days` and before the existing optional `asOfDate` (`consistency(habit, logs, days, schedulePeriods, asOfDate?)`) — appended rather than reordered to match the domain layer's other multi-parameter functions, to keep the change to one insertion per call site rather than a reorder of arguments already in place. `recentHistory()` itself is unchanged, per the working direction: it stays calendar-based, since it powers the heatmap/calendar views, where every calendar day is a legitimate visual artefact regardless of schedule.

**Return type: `number | null`, not a config-driven threshold.** A window with zero Scheduled Opportunities returns `null`, not `0` — matching `averageRecoveryTime`'s existing `number | null` convention in the same domain layer rather than inventing a new return-shape convention. This is a boundary check (`opportunities.length === 0`), not a threshold, and acquired no config value. Every call site was updated to treat `null` as "no percentage to show," not as "0%": `app/(tabs)/progress.tsx`'s per-habit list line and the weekly/monthly aggregate line are each omitted (not rendered) when `null`; `app/habit/[id].tsx`'s Consistency tile is omitted the same way the adjacent Recovery tile already was when unavailable. No new copy was introduced anywhere — every null case is handled by omitting the existing line/tile, the same mechanism already used for `avgRecoveryDays !== null` and `recoveryTile &&` in the same files, not by writing new prose.

**The pause defect — the strongest single argument for this fix, stated explicitly.** A habit paused through the recovery card's own "Pause this habit" action (one tap, no confirmation) was, before this fix, still penalised by Consistency for exactly the days the app itself told the user not to be evaluated on — those days read as misses in the calendar-day denominator despite `isScheduledOpportunity` correctly excluding them from every other domain calculation (Recovery, Momentum). This was live and visible on Progress for any habit ever paused this way, not a hypothetical: the app was penalising a user for using its own feature. `scheduledOpportunitiesInWindow` excludes paused days from the opportunity list entirely (`isScheduledOpportunity` returns `false` under `paused`), so they now contribute to neither numerator nor denominator, resolving it. Covered directly by a new test (`lib/domain/habit-stats.test.ts`, "excludes a paused period from both numerator and denominator").

**Edge Functions: shipped independently, without leaving two functions named alike with different meanings.** Both Edge Functions call `consistency()` live (`send-coaching-push`'s per-habit `consistencyPct` feeding its push copy, `ai-insights`' `consistencyPct` feeding Claude's prompt), and both are generated from `lib/domain/habit-stats.ts` via `scripts/build-edge-functions.js`'s whitelist. Changing `consistency()`'s signature without addressing this would have meant the generator inlining a function requiring `schedulePeriods` into two files whose own call sites don't have it, and that neither function currently fetches (`habit_schedule_periods` — confirmed by grep, zero matches in either file). Per instruction, the actual schedule-aware integration was **not** done. Instead: `consistency()`'s pre-fix body was preserved verbatim under a new, honest name, `calendarConsistency` (same file, documented in its own doc comment as existing only for this reason and not consumed by any client screen); `scripts/build-edge-functions.js`'s `SOURCES` whitelist now inlines `calendarConsistency` instead of `consistency`; both Edge Functions' own call sites were updated from `consistency(...)` to `calendarConsistency(...)`; `npm run build:edge-functions` was re-run so the generated block reflects the rename. Both Edge Functions are behaviorally byte-identical to before this fix — confirmed by `scripts/build-edge-functions.test.ts`'s freshness check passing, which fails if the generated block and the source drift. The client and the Edge Functions now read two distinctly-named functions with two distinct, honestly-labeled meanings, rather than one shared name silently diverging.

**Edge Function real integration — analysis only, not implemented, per instruction:**
1. **What extending the whitelist to inline `schedule.ts` requires, and its transitive dependencies.** Not just `schedule.ts` itself. `isScheduledOpportunity`/`scheduleForDate` depend on `localDayKeyOf`, `weekdayOf`, and `parseDayKeyParts` from `day-key.ts` — none of which are in the current whitelist (only `dayKey`/`addDays` are, per the generator's own comment noting `weekdayOf`/`localDayKeyOf` are deliberately excluded today). So this is a two-file whitelist extension, not one. Separately, `schedule.ts` imports `HabitSchedulePeriod`/`ScheduleDays` as *types* from `../habit-types` — a file outside `lib/domain/` entirely, which this generator has never inlined (both Edge Functions currently hand-shim their own local `Habit`/`HabitLog` types rather than importing `habit-types.ts`). Extending to `schedule.ts` means hand-adding equivalent local `HabitSchedulePeriod`/`ScheduleDays` shims to both Edge Functions, matching that existing convention, since `habit-types.ts` itself isn't a candidate for inlining (it likely carries its own dependencies never audited against `assertNoDisallowedDependencies`). `schedule.ts` itself passes the disallowed-dependency check as it stands (checked directly — only type-only imports from `../habit-types` and `./day-key`).
2. **What the new `habit_schedule_periods` read looks like, including RLS.** The two Edge Functions use different access models today (CLAUDE.md, "AI coaching" section, and the send-coaching-push header comment): `ai-insights` builds a user-scoped client from the caller's forwarded JWT, so a new `supabase.from('habit_schedule_periods').select(...).eq('user_id', userId)` query would go through the same RLS policy (`auth.uid() = user_id`, already live per `supabase/habit_schedule_periods_schema.sql`) its existing `habits`/`habit_logs` reads already rely on — no new policy needed, just a new read on an existing table through an existing gate. `send-coaching-push` instead runs under the service-role key (bypassing RLS by design, since it operates across all users for the cron-triggered digest) — a new `habit_schedule_periods` read there would need explicit `user_id` filtering in the query itself, exactly as its existing `habits`/`habit_logs` queries already must, since RLS provides no protection under a service-role key. Both would need a `toDomainSchedulePeriod`-style snake_case-to-camelCase row mapper, mirroring the existing `toDomainHabit`/`toDomainLogs` pattern already in both files.
3. **Whether this is the same Phase 5 boundary work CLAUDE.md already documents, and whether doing it now shrinks Phase 5 or only front-loads it.** Same boundary, encountered early. CLAUDE.md's "Phase 5 constraint, not yet resolved" section is about `momentum.ts`/`recovery.ts` specifically, but both of those modules take `schedulePeriods` and call into `schedule.ts` internally — so whitelisting `schedule.ts` (plus the `day-key.ts` widening and the type-shim convention above) is a genuine subset of what Phase 5's eventual full-domain-layer boundary work will need regardless. Doing it now would make that specific piece already-done groundwork rather than something Phase 5 reinvents — a real, if narrow, reduction. It would not meaningfully shrink Phase 5's larger remaining work (grounding coaching in `momentum.ts`/`recovery.ts` themselves, plus the locked spec's "Deterministic Architecture" requirements — structured facts only, no raw history to the model, output validation rejecting invented statistics), which is independent of whether `schedule.ts` is already inlined. Net characterization: mostly earlier, marginally smaller. Not undertaken here; Phase 5 has not been approved and this analysis does not recommend starting it.

**Two open questions recorded, not resolved, per instruction:**

- **Heatmap adjacency.** Under the split, a non-daily habit's calendar/heatmap (`recentHistory`, still calendar-based) and its Consistency figure (`consistency`, now schedule-aware) can visibly disagree in a way a reader could perceive as contradictory — most concretely in `app/habit/[id].tsx`, where both currently sit under one shared "last 28 days" label. Neither `HabitCalendar` nor `HabitHeatmap` distinguishes "not a Scheduled Opportunity" from "missed" today; both render only filled/outlined. Options, none chosen: (a) give cells a third visual state for non-scheduled days, which requires `DayStatus` or a sibling shape to carry a schedule-awareness flag and new rendering logic in both components; (b) decouple the shared "last 28 days" label so the two numbers no longer imply describing the same population of days; (c) leave both as-is, extending the same non-contradiction reasoning already accepted for the Momentum/Recovery adjacency (two true, differently-scoped statements) — though this instance sits more tightly adjacent, under a shared label, than that one did; (d) some combination of the above.
- **Bonus completions.** Nothing on Today restricts logging to a habit's own Scheduled-Opportunity days (checked directly — that gate exists only for Recovery Card eligibility, not the checkbox/stepper itself), so a completion logged on a non-scheduled day is, under the new model, excluded from both `consistency()`'s numerator and denominator (the date is absent from `scheduledOpportunitiesInWindow`'s output entirely), though it still counts toward Total Completions and remains in the raw log unmodified. **How many existing habits this affects is not determinable from the codebase** — it would require querying production `habit_logs` for entries whose date wasn't a Scheduled Opportunity for that habit at the time, and this pass has no database access (consistent with the rest of this document's "code + automated checks only" verification method). The structurally-affected population is at least boundable: only habits that have ever had a non-daily schedule or a pause period (i.e., have any `habit_schedule_periods` row with `days !== 'daily'` or `paused === true`) could possibly have a bonus completion at all. Options, none chosen: (a) let them simply not count toward Consistency, as implemented now — the completion still exists and still counts toward Total Completions; (b) have a completion retroactively count as its own Scheduled Opportunity, which would require the schedule layer to read `logs`, breaking its current, deliberate independence from log data; (c) restrict logging itself to Scheduled-Opportunity days at the Today-screen UI level, removing the "bonus rep" flexibility that exists today.
- **Client/Edge Function Consistency divergence, flagged for the Phase 5 precondition review.** The client now displays schedule-aware `consistency()`; both Edge Functions compute `calendarConsistency()` (the preserved pre-fix behavior, per the shim above). Both are individually correct under their own definition, but they are now two different quantities sharing one word across the client/server boundary. This is concretely live in `ai-insights`, which feeds its `calendarConsistency`-derived `consistencyPct` to Claude as part of the coaching prompt: an MWF habit with perfect adherence would show 100% on Progress while the coach is told 43%, sourced from the same word for the same habit. Recorded as an open item for the Phase 5 precondition review rather than fixed here, since that review is already scheduled to resolve how the coach receives Momentum and Recovery data under the same generated-domain boundary constraint (CLAUDE.md, "Phase 5 constraint, not yet resolved") — this is the same class of problem (a domain quantity the Edge Functions can't yet compute correctly because of what the generator whitelist inlines), not a separate one, so it belongs in the same review rather than a standalone fix. Not fixed here, per instruction.

**Tests.** `lib/domain/habit-stats.test.ts`: the three pre-existing `consistency` tests were kept (using `simpleHabit()`, daily and old enough that the schedule-aware and calendar-day models agree, so only an inserted `schedulePeriods` argument was needed), except the zero-day-window case, whose expectation changed from `toBe(0)` to `toBeNull()` with an inline comment explaining why; new cases added for Mon/Wed/Fri exclusion, paused-period exclusion, and the zero-opportunity `null` case. A parallel `calendarConsistency` describe block preserves the original three tests' bodies and assertions unchanged, characterizing the frozen Edge Function shim. `lib/domain/schedule.test.ts` gained a `scheduledOpportunitiesInWindow` describe block (4 cases: agreement with `scheduledOpportunitiesUpTo` when the window covers the full lifetime, the creation floor, the zero-opportunity case, a paused sub-range). `lib/domain/dev-simulation.test.ts:111-141`: all three `consistency(...)` calls gained the `schedulePeriods` argument (`[]`, matching the file's existing `candidateStateAt(..., [], ...)` convention); the `buggy`-habit assertion at (former) line 123 changed from `toBeCloseTo(12/14)` to `toBe(1)`, flagged inline rather than silently adjusted — with `createdAt` pinned to "today," the schedule-aware window has exactly one Scheduled Opportunity, which this history completed, so 1/1 replaces what used to be an unfiltered 12/14; the surrounding comment (previously "Total Completions / Consistency read raw logs by date -- unaffected by createdAt") was corrected, since it's now false for `consistency` specifically and was rewritten to say so.

**No locked fixture affected.** `docs/phase-2-implementation-plan.md` §8's fixture table — the actual locked-fixture mechanism in this codebase — has no Consistency column at all (its columns are Scheduled Opportunities, Recovery Events, Recoverable Lapse Opportunities, Recovery Rate, Recovery Time, and confirmed Momentum State); confirmed directly, not assumed.

**User-visible impact, as characterized before implementation and confirmed unchanged by it:** unchanged for daily, unpaused habits older than the relevant window (the schedule-aware and calendar-day models are mathematically identical in that case — every calendar day in the window is a Scheduled Opportunity); generally higher for non-daily habits, habits with a paused period inside the window, and young habits that already have Scheduled Opportunities in the window; no percentage rather than 0% where the window contains zero Scheduled Opportunities.

**Full verification.** `npm test` — 10 suites, 188 tests, all passing (10 new: 5 in `habit-stats.test.ts`'s schedule-aware/`calendarConsistency` split, 4 in `schedule.test.ts`'s `scheduledOpportunitiesInWindow` block, plus `dev-simulation.test.ts`'s existing cases updated in place as flagged above). `npx tsc --noEmit` — clean. `npm run lint` — clean. `npx expo export --platform web` — bundles cleanly (1287 modules), all 15 static routes present. `scripts/build-edge-functions.test.ts`'s freshness check passes, confirming both regenerated Edge Function files match their source exactly.

**Scope respected.** Phase 5 has not been started and this document does not recommend starting it. No Momentum, Recovery, or schedule/pause semantics were changed — `isScheduledOpportunity`, `scheduleForDate`, and `scheduledOpportunitiesUpTo` are untouched; only a new windowed sibling was added. No copy was introduced; every null-handling change is an omission of an existing line/tile, not new prose. The Edge Functions' actual behavior is unchanged, confirmed by the freshness check.

## Post-completion: pre-`backdatedCreatedAt` data audit and remediation (closes the open item)

**Background.** Commit `c9bac40` ("Fix Phase 4 retroactive entry to not accept pre-creation dates," 2026-08-08) fixed the production retroactive-entry UI path and, separately, an earlier commit (`7bf2cc3`, documented above under "Post-completion manual testing fix") fixed the developer simulation tools' own version of the same defect class: a habit's `created_at` left un-backdated while `habit_logs` were backfilled into the past, making those dates silently invisible to every Scheduled-Opportunity-based calculation (Momentum, Recovery, and now schedule-aware Consistency) while still counting toward Total Completions. `c9bac40` also added `supabase/audit_pre_creation_logs.sql`, a read-only query to check whether any pre-fix production data already existed in this shape. No record existed anywhere in this repository of that query having actually been run until now.

**Audit run and results reported 2026-08-17.** The audit was run against the live database. Result: **103 offending `habit_logs` rows across 7 habits and 3 user IDs** — every offending date at least several days before its habit's `created_at`, several as much as 29 days, ruling out the query's own documented timezone-boundary caveat (a one-day false positive/negative at the UTC-vs-device-local boundary) as the explanation. Confirmed, not assumed: these are genuine developer-simulation history, created before `backdatedCreatedAt` existed, by the account owner's own testing. The logs themselves were never wrong — only `created_at` was left behind, exactly the defect class `backdatedCreatedAt` and `retroactiveEntryWindowStart` both exist to prevent going forward.

**Both write paths that could produce this are structurally closed, confirmed against current code, not assumed from the historical fix commits alone:**
- **Production retroactive-entry UI.** `app/habit/[id].tsx` still passes `minEditableDate={retroactiveEntryWindowStart(habit, today)}` into `HabitCalendar`, and `retroactiveEntryWindowStart` (`lib/domain/schedule.ts`) still takes the later of the fixed 7-day window and the habit's own local creation day. Nothing in the Consistency work earlier in this document touched this function or its call site.
- **Developer simulation tools.** All five debug reducer cases in `lib/habit-store.tsx` route through `withSimulatedHistory`, which calls `backdatedCreatedAt(habit.createdAt, simulatedDates)` before any log is written, using the *same* date array for both the backdating and the log generation (`simulatedLogsFor`) — so every simulated log's date is structurally covered by the resulting `created_at` by construction, not by convention. Directly tested and passing: `lib/domain/dev-simulation.test.ts`'s "simulated history respects the habit creation-date floor" case.

**Remediation: `supabase/remediate_pre_creation_logs.sql`, written, not run.** A one-time corrective migration, scoped explicitly to the 7 offending habit IDs (not a general predicate), per instruction. Decision: backdate `created_at` to each habit's earliest offending log date, do not delete — the data is genuine deliberately-built test history, and deleting it would discard exactly what was built to validate the original `backdatedCreatedAt`/`retroactiveEntryWindowStart` fixes. The corrected date per habit is the earliest offending `habit_logs.date` already surfaced by the audit, which is exactly what `backdatedCreatedAt` itself would compute (the minimum of every simulated date for that habit — any log on/after the pre-correction `created_at` is by definition not offending, so cannot be earlier than the offending set).

**Time-of-day semantics: reported and disclosed, not silently approximated, per instruction.** `backdatedCreatedAt` preserves *device-local* wall-clock time-of-day (via JS's local `Date` constructor on both ends). Postgres `timestamptz` retains no per-row timezone/offset metadata, and none was ever recorded for these pre-fix rows, so there is no way to reconstruct which device-local timezone was in effect when each was originally set — faithfully reproducing `backdatedCreatedAt`'s literal semantics in SQL is not possible here. The migration instead preserves the stored *UTC* time-of-day exactly (via explicit `AT TIME ZONE 'UTC'`, not the SQL session's ambient `TimeZone` setting) and substitutes only the UTC calendar date — a disclosed deviation, safe here because all 7 original timestamps sit comfortably away from UTC midnight (07:11:30–21:47:32 UTC), so no plausible device timezone offset flips which calendar date the correction resolves to. Documented at length in the migration file's own header.

**`habit_schedule_periods`: traced, not assumed, no second-order inconsistency found.** `scheduleForDate` (`lib/domain/schedule.ts`) falls back to its own documented default (`{ days: 'daily', paused: false }`) for any date before a habit's first explicit period — the same default a zero-period habit gets everywhere. Backdating `created_at` therefore cannot create a gap with undefined schedule behavior between the corrected creation date and any first explicit period, regardless of whether one exists for these 7 habits or what it says. This holds structurally, independent of this pass's database access limits.

**`updated_at`: bumped to `now()` at migration time, required for correctness, not cosmetic.** Traced `lib/supabase-sync.ts`'s `pullRemoteChanges` and `lib/habit-store.tsx`'s `mergeRemote` reducer case directly. `mergeRemote` merges by `if (habit.updatedAt >= habits[index].updatedAt) habits[index] = habit` (last-write-wins, ties to the incoming row), and `pullRemoteChanges` only fetches rows with `updated_at` past a per-device watermark. Leaving `updated_at` unbumped would risk two independent failure modes: a device whose watermark already exceeds the old value never even fetching the correction, or a device's stale local copy winning the merge outright. `now()` is the only value guaranteed newer than every device's current watermark and local copy.

**One data-integrity flag, not resolved by assumption.** Of the 7 reported habit IDs, 6 are well-formed UUIDs; one (`80fa9ad6-9912-479d-b83a-2b23897a63f`, the "Drink Water" habit under `4f7549e0-...`) has an 11-character final segment instead of the standard 12 — checked programmatically against all 7, this is the only one that fails. Not guessed or silently corrected; included verbatim in the migration with the discrepancy flagged in both the file and this report. If it's a transcription artifact, that row's `UPDATE` will match zero rows (a safe no-op) and the migration's own verification query will still show it offending afterward.

**User identity: not determinable from anything visible to this pass.** No database access exists to `auth.users` or any identifying metadata for the 3 user IDs. Reported as not determinable, not assumed to match the account owner's stated belief.

**Not run, at the time this section was first written.** The migration was written and reviewed but not executed in this pass — it was intended to be run manually in the Supabase SQL Editor, the same way the audit query itself was run. **Status: applied. See "Migration applied 2026-08-18" immediately below** for the run, its results, and resolution of the two items left open above (the malformed habit ID, and user identity).

**This closes the open item.** Both write paths are confirmed structurally closed in current code (not merely historically fixed), the only pre-fix production data has been located and quantified, and a reviewed, scoped, reversible remediation exists. Nothing about this item is deferred to a later phase or left to be re-asked during the acceptance pass.

**Verification.** `npm test`, `npx tsc --noEmit`, `npm run lint` — no application code changed in this pass (only a new `.sql` file and this document), run anyway per instruction; results in the commit this section accompanies.

### Migration applied 2026-08-18: results and closure

**The two items left open above are both resolved, before the migration ran.**
- **Malformed habit ID, corrected.** The "Drink Water" habit ID under user `4f7549e0-...` was re-read directly from the database and corrected from the transcribed `80fa9ad6-9912-479d-b83a-2b23897a63f` (11-character final segment) to `80fa9ad6-9912-479d-b83a-2b23897a63ff` (12-character final segment). Re-validated programmatically against the standard 8-4-4-4-12 UUID shape, along with all 7 IDs together — all 7 confirmed well-formed. `supabase/remediate_pre_creation_logs.sql` was updated in all three places the ID appears (the migration's `VALUES`, the verification query's `IN` list, and the commented-out rollback), and the file's header caveat was rewritten to record the correction rather than left as a stale "confirm before running" warning.
- **User identity, confirmed by the account owner.** All 3 user IDs are the account owner's own accounts — confirmed directly by them, not inferred from any data visible to this pass (which, as recorded above, was nothing beyond the audit's own output). No habit needed excluding on identity grounds.

**Migration run 2026-08-18, in the Supabase SQL Editor, by the account owner — not by this pass, per instruction throughout.** Result: **7 rows updated** (`habits.created_at` backdated per habit, `updated_at` bumped to the execution-time `now()`, exactly as the migration specified). The migration's own verification query (scoped to the 7 corrected habit IDs) **returned 0 rows** — no habit among the 7 still has any `habit_logs` row dated before its (now-corrected) `created_at`. The account owner additionally re-ran the full, unscoped `supabase/audit_pre_creation_logs.sql` — the same query that originally surfaced the 103 offending rows — and it **also returned 0 rows**, table-wide, not just across the 7 corrected habits. Between the two, this confirms both that the correction worked for the specific rows it targeted and that no other pre-`backdatedCreatedAt` data exists anywhere in the database outside the 7 habits already found and corrected.

**Open item status: closed, not pending.** Both write paths were already confirmed structurally closed in current code (recorded above); the only pre-fix production data has now been located, quantified, and corrected; the migration is applied and independently verified two ways (scoped check and full table-wide re-audit); user identity is confirmed. Nothing here is deferred to the acceptance pass or a later phase.

## Post-completion: calendar/heatmap third cell state (Habit Detail only), and the off-schedule-logging product decision

**Product decision, recorded so it is not reopened as a defect later: extra completions on non-scheduled days remain permitted, on both the live and retroactive paths.** Investigated as part of designing this fix: gating `HabitCalendar`'s retroactive-entry editability by `scheduled` would have closed only the retroactive (past-day) path, since `app/(tabs)/index.tsx`'s Today-screen checkbox/stepper has no schedule gating at all (checked directly — the only schedule-aware check on Today gates Recovery Card eligibility, not logging). Closing one path and not the other would have left a worse asymmetry than either consistent rule (log a bonus completion live on a non-scheduled day, but be unable to record having done so retroactively for the same kind of day). Decision: neither path is gated. Current behaviour was already correct for this case before any of this work started — such logs count toward Total Completions and are already correctly excluded from Scheduled Opportunity adherence (Consistency, Momentum, Recovery) — so nothing needed to change there; this section exists only to record the decision explicitly. `isEditable` in `components/habit-calendar.tsx` keeps its original three conditions unchanged; `scheduled` was deliberately not added to it.

**Shared classification: `scheduledOpportunityFlags`, `lib/domain/schedule.ts`, beside `scheduledOpportunitiesInWindow`.** Returns `{ date, scheduled }[]` for a trailing window, keyed by date rather than position, calling `isScheduledOpportunity` per date rather than re-deriving any part of its creation-floor/pause/weekday ordering. `recentHistory`/`DayStatus` are untouched, per instruction — the separation between "what happened" (calendar-based) and "was it asked for" (schedule-based) stays exactly where the Consistency work left it. Paused dates and ordinary off-schedule dates both resolve to `scheduled: false` identically, since `isScheduledOpportunity` itself doesn't distinguish them and the product decision is that they share one presentation state. Five new tests in `lib/domain/schedule.test.ts`: agreement with `scheduledOpportunitiesInWindow`, a non-daily (Mon/Wed/Fri) schedule, a paused sub-range, a pre-creation date, and the degenerate case of a period whose `effective_from` predates the habit's own creation (proving the creation floor still wins, per `isScheduledOpportunity`'s own doc comment).

**Merge: `app/habit/[id].tsx`, at the screen level, before the `history` prop reaches `HabitCalendar`.** `scheduledOpportunityFlags`'s output is built into a `Map` keyed by date, then `recentHistory`'s own output is mapped over to attach each day's `scheduled` flag by date lookup — not positional index, consistent with why the domain function is keyed by date in the first place. `today` was hoisted earlier in the component (previously declared after `habitConsistency`) so it's available for this computation.

**Third render branch: `components/habit-calendar.tsx`, `HabitCalendar` only — `HabitHeatmap`/Progress deliberately not touched this pass**, per instruction, pending an on-device legibility check at 16px before deciding whether the same treatment scales down or needs its own answer. `HabitCalendar`'s prop type changed from `history: DayStatus[]` to `history: ScheduledDayStatus[]` (a local `type ScheduledDayStatus = DayStatus & { scheduled: boolean }`, exported nowhere else since only this component and its one caller need it). Visual treatment, designed to recede rather than compete, reusing existing colour tokens rather than introducing a new one: a day that is both unscheduled and not done gets a fainter border (`emptyColor` at a lower alpha, `'20'` instead of the existing miss state's `'33'`) and a dimmed day-number (`emptyColor` instead of `textColor`) — the same muted tone already used for the weekday header row above the grid, not a new colour. A completion logged on a non-scheduled day (a permitted bonus completion, per the decision above) still renders as done — the receded treatment only applies when `!scheduled && !done`, so a real logged fact is never visually suppressed. `isToday`'s existing border highlight is unaffected by the new state; `isEditable` is unaffected, per the decision above.

**Off-schedule log count: not run by this pass, committed for the account owner to run.** `supabase/audit_off_schedule_logs.sql`, alongside the existing audit and remediation files — a best-effort SQL replication of `scheduleForDate`'s effective-dated period resolution (documented in its own header as indicative, not authoritative, and as expecting `pre_creation_count` to now read 0 given the already-applied remediation above). No decision made about what, if anything, to do with whatever it finds — reporting only, per instruction.

**Labels: proposed, approved, applied.** Two wordings were proposed for decoupling Habit Detail's percentage-tile caption from the calendar-section heading, both em-dash-free and stated as fact rather than loss, per the copy-approval contract: (1) reword only the tile caption, leaving the grid heading and tap-instruction line untouched, since the heading was never inaccurate on its own and the grid's own visual form already reads as a calendar; (2) reword both sides, appending "calendar" to the grid heading for explicit symmetry. Option 1 was approved and applied — `app/habit/[id].tsx`'s Consistency-tile caption changed from `last {HISTORY_DAYS} days` to `of scheduled days`; the grid's own "Last {HISTORY_DAYS} days" heading and its tap-instruction line are unchanged. Option 2 was explicitly rejected as introducing a distinction ("calendar days") most readers don't need and inviting the question of what other kind of day exists. Progress's heatmap caption remains explicitly out of scope for this pass, since that screen has no existing grid caption to reword and adding one is bundled with the deferred `HabitHeatmap` decision above.

**Full verification.** `npm test` — 10 suites, 193 tests, all passing (5 new, all in `schedule.test.ts`'s `scheduledOpportunityFlags` block). `npx tsc --noEmit` — clean. `npm run lint` — clean. `npx expo export --platform web` — bundles cleanly (1290 modules), all 15 static routes present.

**Scope respected.** Phase 5 has not been started and this document does not recommend starting it. No Momentum, Recovery, Consistency, or existing schedule-primitive semantics changed — `isScheduledOpportunity`, `scheduleForDate`, `scheduledOpportunitiesUpTo`, `scheduledOpportunitiesInWindow`, and `recentHistory`/`DayStatus` are all untouched; only a new sibling function was added. No copy was changed. `HabitHeatmap` and its Progress call site are untouched, pending the on-device review named above.
